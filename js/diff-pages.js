import { escapeHtml } from './utils.js';
const h=(v)=>escapeHtml(String(v??''));
const icon=(n)=>`<i class="fa-solid ${n}" aria-hidden="true"></i>`;
const opt=(v,label,current,disabled=false)=>`<option value="${h(v)}" ${v===current?'selected':''} ${disabled?'disabled':''}>${h(label)}</option>`;
const impactIcon={breaking:'fa-circle-exclamation','potentially-breaking':'fa-triangle-exclamation','review-required':'fa-magnifying-glass',deprecation:'fa-clock','non-breaking':'fa-circle-plus',documentation:'fa-file-lines',metadata:'fa-tag',none:'fa-circle-check'};
const changeIcon={added:'fa-plus',removed:'fa-minus',modified:'fa-pen',unchanged:'fa-equals'};
function impactBadge(v){return `<span class="diff-impact diff-impact--${h(v)}">${icon(impactIcon[v]||'fa-circle-info')} ${h(v)}</span>`;}
function changeBadge(v){return `<span class="diff-change diff-change--${h(v)}">${icon(changeIcon[v]||'fa-circle-info')} ${h(v)}</span>`;}
function pretty(v){if(v===undefined)return '<span class="diff-empty-value">absent</span>';if(v===null)return '<code>null</code>';if(typeof v==='string')return `<code>${h(v)}</code>`;return `<pre class="diff-inline-json"><code>${h(JSON.stringify(v,null,2))}</code></pre>`;}

function releaseOptions(engine,current){return engine.releases.map(r=>opt(r.version,`v${r.version} · Phase ${r.phase}${r.available?'':' · unavailable'}`,current,!r.available)).join('');}
function filterOptions(values,current,placeholder){return `<option value="">${h(placeholder)}</option>${values.map(v=>opt(v,v,current)).join('')}`;}

function summaryCards(result){const s=result.summary||{};return `<section class="dashboard-grid dashboard-grid--4 diff-summary-grid">
<article class="paper-card metric-card"><div class="metric-card__label">Added</div><div class="metric-card__value">${s.added??0}</div><p class="metric-card__meta">New canonical items</p></article>
<article class="paper-card metric-card"><div class="metric-card__label">Removed</div><div class="metric-card__value">${s.removed??0}</div><p class="metric-card__meta">Missing in target release</p></article>
<article class="paper-card metric-card"><div class="metric-card__label">Modified</div><div class="metric-card__value">${s.modified??0}</div><p class="metric-card__meta">Same IDs with changes</p></article>
<article class="paper-card metric-card"><div class="metric-card__label">Recommendation</div><div class="metric-card__value metric-card__value--text diff-recommendation">${h(result.recommendedVersionImpact||'none')}</div><p class="metric-card__meta">${result.requiresManualReview?'Manual review required':'Structural review complete'}</p></article>
</section>`;}

function impactSummary(result){const s=result.summary||{};return `<section class="paper-card"><div class="paper-card__header"><div><h2>Compatibility impact</h2><p>Conservative automated classification. It is a review aid, not a compatibility certificate.</p></div></div><div class="paper-card__body"><div class="diff-impact-summary">
${['breaking','potentially-breaking','review-required','deprecation','non-breaking','documentation','metadata'].map(k=>`<div><span>${impactBadge(k)}</span><strong>${s[k]??0}</strong></div>`).join('')}
</div>${result.requiresManualReview?`<div class="callout callout--warning">${icon('fa-triangle-exclamation')}<div><strong>Manual compatibility review required</strong><p>At least one change could not be classified as safely backward-compatible from structure alone.</p></div></div>`:`<div class="callout callout--success">${icon('fa-circle-check')}<div><strong>No unresolved review classification detected</strong><p>Still verify affected implementation behavior before upgrading a production Site.</p></div></div>`}</div></section>`;}

function releaseProvenance(engine,from,to){const a=engine.release(from),b=engine.release(to);const card=(r,label)=>`<article class="diff-release-card"><span>${h(label)}</span><strong>v${h(r?.version||'')}</strong><small>Phase ${h(r?.phase??'')} · ${h(r?.availability||'unknown')}</small><code>${h((r?.registrySha256||'').slice(0,16))}${r?.registrySha256?'…':''}</code><p>${h(r?.sourceReference||r?.notes||'No source')}</p></article>`;return `<section class="paper-card"><div class="paper-card__header"><div><h2>Release provenance</h2><p>Only exact available Registry snapshots are eligible for deterministic comparison.</p></div></div><div class="paper-card__body"><div class="diff-release-grid">${card(a,'From')}${card(b,'To')}</div></div></section>`;}

function changesList(result,filters){if(result.error)return `<div class="callout callout--danger">${icon('fa-circle-exclamation')}<div><strong>Comparison unavailable</strong><p>${h(result.error)}</p></div></div>`;if(!result.changes.length)return `<div class="empty-state"><i class="fa-solid fa-circle-check" aria-hidden="true"></i><h3>No matching changes</h3><p>Adjust the filters or include unchanged items.</p></div>`;return `<div class="diff-change-list">${result.changes.slice(0,250).map(d=>`<article class="diff-item-row"><div class="diff-item-row__main"><div class="diff-item-row__title"><a href="#/registry/item/${encodeURIComponent(d.itemId)}">${h(d.name)}</a><code>${h(d.itemId)}</code></div><div class="diff-item-row__meta"><span>${h(d.domain)}</span><span>${h(d.type)}</span>${changeBadge(d.changeKind)}${impactBadge(d.impact)}</div></div><div class="diff-item-row__action"><a class="button button--secondary button--compact" href="#/lifecycle/diff?from=${encodeURIComponent(result.fromVersion)}&to=${encodeURIComponent(result.toVersion)}&item=${encodeURIComponent(d.itemId)}${filters.includeUnchanged?'&unchanged=1':''}">${icon('fa-code-compare')} Inspect</a></div></article>`).join('')}</div>${result.changes.length>250?`<p class="relationship-limit-note">Showing the first 250 of ${result.changes.length} filtered items. Narrow the filters for a smaller review set.</p>`:''}`;}

function fieldChanges(detail){const rows=detail.pathChanges.filter(x=>x.field);if(!rows.length)return '<p class="muted">No keyed field changes detected.</p>';return `<div class="table-wrap"><table class="data-table"><thead><tr><th>Field</th><th>Change</th><th>Impact</th><th>Before</th><th>After</th></tr></thead><tbody>${rows.map(x=>`<tr><td><code>${h(x.field)}</code></td><td><strong>${h(x.changeType)}</strong><small>${h(x.summary)}</small></td><td>${impactBadge(x.impact)}</td><td>${pretty(x.before)}</td><td>${pretty(x.after)}</td></tr>`).join('')}</tbody></table></div>`;}
function groupedChanges(detail,needle,label){const rows=detail.pathChanges.filter(x=>x.changeType.includes(needle));if(!rows.length)return `<p class="muted">No ${h(label.toLowerCase())} changes detected.</p>`;return `<div class="diff-detail-stack">${rows.map(x=>`<div class="diff-path-row"><div><strong>${h(x.changeType)}</strong><code>${h(x.path)}</code></div><div>${impactBadge(x.impact)}</div><p>${h(x.summary)}</p></div>`).join('')}</div>`;}
function rawChanges(detail){const rows=detail.pathChanges.slice(0,300);if(!rows.length)return '<p class="muted">No structural path changes.</p>';return `<div class="diff-raw-list">${rows.map(x=>`<article class="diff-raw-row"><div class="diff-raw-row__head"><code>${h(x.path||'(item)')}</code>${impactBadge(x.impact)}</div><p>${h(x.summary)}</p><div class="diff-before-after"><div><span>Before</span>${pretty(x.before)}</div><div><span>After</span>${pretty(x.after)}</div></div></article>`).join('')}</div>${detail.pathChanges.length>300?'<p class="relationship-limit-note">Raw path output is bounded to 300 changes.</p>':''}`;}
function history(engine,detail){if(!detail.history.length)return '<p class="muted">No exact release history is available for this item.</p>';return `<div class="diff-history">${detail.history.map((x,i)=>`<div class="diff-history__entry"><span class="diff-history__dot"></span><div><strong>v${h(x.release.version)}</strong><small>Phase ${h(x.release.phase)} · contract ${h(x.manifest.version||'—')} · ${h(x.manifest.status)}</small><code>${h((x.manifest.snapshotHash||x.manifest.sourceHash||'').slice(0,14))}${x.manifest.snapshotHash||x.manifest.sourceHash?'…':''}</code></div></div>`).join('')}</div>`;}

function detailPanel(engine,detail,filters){if(!detail)return '';return `<section class="paper-card diff-detail-card" id="diff-detail"><div class="paper-card__header"><div><div class="eyebrow">Contract change detail</div><h2>${h(detail.name)}</h2><code>${h(detail.itemId)}</code></div><div class="header-actions">${changeBadge(detail.changeKind)}${impactBadge(detail.impact)}<a class="button button--secondary button--compact" href="#/registry/item/${encodeURIComponent(detail.itemId)}">${icon('fa-arrow-up-right-from-square')} Canonical contract</a></div></div><div class="paper-card__body"><div class="diff-detail-overview"><div><span>From</span><strong>v${h(filters.from)}</strong><small>${h(detail.from?.version||'not present')}</small></div><div><span>To</span><strong>v${h(filters.to)}</strong><small>${h(detail.to?.version||'not present')}</small></div><div><span>Recommended impact</span><strong>${h(detail.recommendation)}</strong><small>${detail.pathChanges.length} detected path change${detail.pathChanges.length===1?'':'s'}</small></div><div><span>Highest impact</span>${impactBadge(detail.impact)}</div></div><div class="diff-tabs"><a href="#diff-fields">Fields</a><a href="#diff-relationships">Relationships</a><a href="#diff-permissions">Permissions</a><a href="#diff-events">Events</a><a href="#diff-validation">Validation</a><a href="#diff-raw">Raw paths</a><a href="#diff-history">History</a></div><section id="diff-fields" class="diff-section"><h3>Field changes</h3>${fieldChanges(detail)}</section><section id="diff-relationships" class="diff-section"><h3>Relationship changes</h3>${groupedChanges(detail,'relationship','Relationship')}</section><section id="diff-permissions" class="diff-section"><h3>Permission changes</h3>${groupedChanges(detail,'permission','Permission')}</section><section id="diff-events" class="diff-section"><h3>Event changes</h3>${groupedChanges(detail,'event','Event')}</section><section id="diff-validation" class="diff-section"><h3>Validation changes</h3>${detail.pathChanges.filter(x=>x.changeType.includes('validation')).length?groupedChanges(detail,'validation','Validation'):'<p class="muted">No validation changes detected.</p>'}</section><section id="diff-raw" class="diff-section"><h3>Raw structural paths</h3>${rawChanges(detail)}</section><section id="diff-history" class="diff-section"><h3>Exact release history</h3>${history(engine,detail)}</section></div></section>`;}

function availability(engine){const missing=engine.releases.filter(x=>!x.available);return `<section class="paper-card"><div class="paper-card__header"><div><h2>Historical snapshot coverage</h2><p>Unavailable releases are visible but cannot be selected for an exact comparison.</p></div><span class="tag">${engine.availableReleases().length} exact</span></div><div class="paper-card__body"><div class="diff-availability-list">${engine.releases.map(r=>`<div class="diff-availability-row"><div><strong>v${h(r.version)}</strong><small>Phase ${h(r.phase)}</small></div><span class="status-badge ${r.available?'status-badge--stable':'status-badge--planned'}">${h(r.available?'Exact snapshot':r.availability)}</span><span>${r.available?`${h(r.itemCount)} items`:h(r.notes)}</span></div>`).join('')}</div>${missing.length?`<div class="callout callout--info">${icon('fa-circle-info')}<div><strong>${missing.length} historical release${missing.length===1?' is':'s are'} unavailable for exact diff</strong><p>The system intentionally does not reconstruct missing archives and call them authoritative.</p></div></div>`:''}</div></section>`;}

export function renderDiffPage(engine,filters){
 const result=engine.compare(filters.from,filters.to,filters);const domains=[...new Set([...Object.values(engine.manifest(filters.from)?.items||{}),...Object.values(engine.manifest(filters.to)?.items||{})].map(x=>x.domain).filter(Boolean))].sort();const types=[...new Set([...Object.values(engine.manifest(filters.from)?.items||{}),...Object.values(engine.manifest(filters.to)?.items||{})].map(x=>x.type).filter(Boolean))].sort();const detail=filters.item?engine.detail(filters.item,filters.from,filters.to):null;
 const current=engine.release(engine.currentVersion)??engine.releases.at(-1);
 return `<header class="page-header"><div><div class="eyebrow">Lifecycle / Contract Diff</div><h1>Contract Diff</h1><p>Compare two exact NEXT F Contract Registry releases, detect structural changes, classify compatibility impact and review one contract down to field-level changes.</p></div><div class="header-actions"><span class="status-badge status-badge--stable">Phase ${h(current?.phase??37)}</span><span class="tag">v${h(current?.version??engine.currentVersion)}</span><a class="button button--secondary button--compact" href="./standards/33-contract-diff-standard.md">${icon('fa-file-lines')} Standard</a></div></header><div class="stack stack--lg"><div class="registry-source-bar"><span>${icon('fa-code-compare')} Diff source</span><strong>${h(engine.source==='generated-fallback'?'Generated compressed local fallback':'Authoritative release JSON')}</strong></div><section class="paper-card"><div class="paper-card__header"><div><h2>Compare Registry releases</h2><p>Only exact snapshots are selectable. Comparison never modifies a Site or executes a migration.</p></div><button class="button button--ghost button--compact" type="button" data-diff-swap>${icon('fa-right-left')} Swap</button></div><div class="paper-card__body"><div class="diff-controls"><label class="field"><span>From release</span><select data-diff-from>${releaseOptions(engine,filters.from)}</select></label><label class="field"><span>To release</span><select data-diff-to>${releaseOptions(engine,filters.to)}</select></label><label class="field diff-search-field"><span>Search changes</span><input type="search" value="${h(filters.q)}" placeholder="commerce.order, Blog Post..." data-diff-q></label><label class="field"><span>Domain</span><select data-diff-domain>${filterOptions(domains,filters.domain,'All domains')}</select></label><label class="field"><span>Type</span><select data-diff-type>${filterOptions(types,filters.type,'All types')}</select></label><label class="field"><span>Impact</span><select data-diff-impact>${filterOptions(engine.impactLevels.levels.map(x=>x.id),filters.impact,'All impacts')}</select></label><label class="field"><span>Item change</span><select data-diff-change>${filterOptions(['added','removed','modified','unchanged'],filters.change,'All item changes')}</select></label><label class="check-field"><input type="checkbox" ${filters.includeUnchanged?'checked':''} data-diff-unchanged><span>Include unchanged items</span></label></div></div></section>${summaryCards(result)}${impactSummary(result)}${releaseProvenance(engine,filters.from,filters.to)}<section class="paper-card"><div class="paper-card__header"><div><h2>Changed Registry items</h2><p><strong data-diff-count>${result.totalFiltered??0}</strong> matching items from ${result.totalCompared??0} compared canonical IDs.</p></div><button class="button button--ghost button--compact" type="button" data-diff-clear>${icon('fa-rotate-left')} Reset filters</button></div><div class="paper-card__body" data-diff-results>${changesList(result,filters)}</div></section>${detailPanel(engine,detail,filters)}${availability(engine)}<section class="paper-card"><div class="paper-card__header"><div><h2>Interpretation guardrails</h2><p>Automated structural analysis is deliberately conservative.</p></div></div><div class="paper-card__body"><ul class="list-clean"><li class="list-item"><span class="list-item__icon">${icon('fa-shield-halved')}</span><div><strong>Diff is not compatibility certification</strong><p>Runtime and business behavior still require implementation review.</p></div></li><li class="list-item"><span class="list-item__icon">${icon('fa-box-archive')}</span><div><strong>No fake history</strong><p>Historical versions without an exact archived Registry snapshot remain unavailable.</p></div></li><li class="list-item"><span class="list-item__icon">${icon('fa-code-branch')}</span><div><strong>No automatic migration</strong><p>Nothing in this screen changes a Site Manifest, database or deployed website.</p></div></li><li class="list-item"><span class="list-item__icon">${icon('fa-robot')}</span><div><strong>Codex must review affected changes</strong><p>Breaking and review-required changes must be addressed in the implementation plan before a Contract Version upgrade.</p></div></li></ul></div></section></div>`;
}

export function renderDiffResults(engine,filters){const result=engine.compare(filters.from,filters.to,filters);return {html:changesList(result,filters),count:result.totalFiltered,result};}
