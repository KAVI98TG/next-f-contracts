const e=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
const icon=n=>`<i class="fa-solid ${n}" aria-hidden="true"></i>`;
const link=id=>`#/registry/item/${encodeURIComponent(id)}`;
const header=(t,d,a="")=>`<header class="page-header"><div><div class="eyebrow">Platform / NEXT F Admin UI</div><h1>${e(t)}</h1><p>${e(d)}</p></div>${a}</header>`;
const chips=(arr=[])=>arr.length?`<div class="module-chip-list">${arr.map(x=>`<a class="tag" href="${link(x)}"><code>${e(x)}</code></a>`).join("")}</div>`:`<p class="empty-note">None.</p>`;
function card(x,view){
 if(view==="controls")return `<a class="paper-card adminui-card" href="${link(x.$id)}"><span class="adminui-icon">${icon("fa-sliders")}</span><h3>${e(x.label)}</h3><p>${e(x.description)}</p><code>${e(x.id)}</code></a>`;
 if(view==="panels")return `<a class="paper-card adminui-card" href="${link(x.$id)}"><span class="adminui-icon">${icon("fa-table-columns")}</span><h3>${e(x.label)}</h3><p>${e(x.description)}</p><code>${e(x.id)}</code></a>`;
 if(view==="schemas")return `<a class="paper-card adminui-card" href="${link(x.$id)}"><span class="adminui-icon">${icon("fa-diagram-project")}</span><h3>${e(x.name)}</h3><p>${e(x.description)}</p><code>${e(x.$id)}</code></a>`;
 return `<a class="paper-card adminui-card" href="${link(x.$id)}"><div class="adminui-card__top"><span class="adminui-icon">${icon(x.profileType==="admin-shell"?"fa-shield-halved":x.screenKind==="admin-report"?"fa-chart-line":x.screenKind==="admin-queue"?"fa-list-check":"fa-screwdriver-wrench")}</span><span class="status-badge status-badge--stable">${e(x.screenKind)}</span></div><h3>${e(x.name)}</h3><p>${e(x.description)}</p><div class="adminui-meta"><span><code>${e(x.resource)}</code></span><span>${e(x.context?.kind||"platform")}</span><span>${x.permissions?.length??0} perms</span></div></a>`;
}
export function renderAdminUiResults(engine,f){const rows=engine.search(f.q,f);if(!rows.length)return `<div class="empty-state"><i class="fa-solid fa-filter-circle-xmark" aria-hidden="true"></i><h3>No Admin metadata matches</h3><p>Clear filters or search another internal resource.</p></div>`;return `<div class="adminui-grid">${rows.map(x=>card(x,f.view)).join("")}</div>`;}
export function renderAdminUiIndex(engine,f){
 const kinds=engine.data.screenKinds.map(x=>`<option value="${e(x.id)}" ${f.kind===x.id?"selected":""}>${e(x.label)}</option>`).join("");
 const domains=[...new Set(engine.profiles.map(x=>x.permissionDomain).filter(Boolean))].sort().map(x=>`<option value="${e(x)}" ${f.domain===x?"selected":""}>${e(x)}</option>`).join("");
 const contexts=engine.data.contextKinds.map(x=>`<option value="${e(x.id)}" ${f.context===x.id?"selected":""}>${e(x.label)}</option>`).join("");
 return `${header("NEXT F Admin UI Metadata","Internal-only UI contracts for managing Organizations, Sites, contracts, migrations, diagnostics, access, integrations and every authorized Site resource without exposing platform controls to customers.",`<div class="header-actions"><span class="status-badge status-badge--stable">Phase 20</span><span class="tag">v${e(engine.data.registryVersion)}</span><a class="button button--secondary button--compact" href="./standards/28-nextf-admin-ui-metadata-standard.md">${icon("fa-file-lines")} Standard</a></div>`)}
 <div class="stack stack--lg"><div class="registry-source-bar" role="status"><span>${icon("fa-database")} Runtime source</span><strong>${engine.source==="authoritative-json"?"Authoritative NEXT F Admin UI JSON":"Generated local fallback"}</strong></div>
 <section class="dashboard-grid dashboard-grid--4"><article class="metric-card"><span>Admin profiles</span><strong>${engine.profiles.length}</strong><small>Shell + complete Admin resource coverage</small></article><article class="metric-card"><span>Controls</span><strong>${engine.controls.length}</strong><small>Privileged reusable controls</small></article><article class="metric-card"><span>Panels</span><strong>${engine.panels.length}</strong><small>Internal technical context panels</small></article><article class="metric-card"><span>Support schemas</span><strong>${engine.schemas.length}</strong><small>Reusable Admin metadata structures</small></article></section>
 <section class="paper-card"><div class="paper-card__header"><div><h2>Admin authority chain</h2><p>The interface never overrides canonical security or business authority.</p></div></div><div class="paper-card__body"><div class="manifest-flow"><span>Admin Identity</span><b>→</b><span>Scope</span><b>→</b><span>Permission</span><b>→</b><span>Canonical Contract / Rule</span><b>→</b><span>Admin UI Profile</span><b>→</b><span>Server Command</span></div></div></section>
 <section class="callout"><span class="callout__icon">${icon("fa-user-shield")}</span><div><h3>No hidden customer impersonation</h3><p>Support navigation uses the authenticated NEXT F identity. A future impersonation feature would require its own explicit security/audit contract.</p></div></section>
 <section class="callout"><span class="callout__icon">${icon("fa-server")}</span><div><h3>Hosting-independent Site administration</h3><p>Site 360 may show URLs, environments, API/runtime connections and health. NEXT F Admin does not become a domain registrar, hosting reseller, renewal system or generic hosting control panel.</p></div></section>
 <section class="paper-card"><div class="paper-card__header"><div><h2>Explore Admin metadata</h2><p>Inspect profiles, internal panels, privileged controls and reusable Admin UI support schemas.</p></div><button class="button button--ghost button--compact" data-adminui-clear>${icon("fa-rotate-left")} Clear</button></div><div class="paper-card__body"><div class="registry-toolbar"><label class="search-field">${icon("fa-magnifying-glass")}<input data-adminui-q type="search" value="${e(f.q)}" placeholder="Search Site 360, migrations, payments, webhooks..."></label><select data-adminui-view><option value="profiles" ${f.view==="profiles"?"selected":""}>Admin profiles</option><option value="controls" ${f.view==="controls"?"selected":""}>Controls</option><option value="panels" ${f.view==="panels"?"selected":""}>Panels</option><option value="schemas" ${f.view==="schemas"?"selected":""}>Support schemas</option></select><span data-adminui-profile-filters><select data-adminui-kind><option value="">All screen kinds</option>${kinds}</select><select data-adminui-domain><option value="">All permission domains</option>${domains}</select><select data-adminui-context><option value="">All contexts</option>${contexts}</select></span><div class="field-filter-summary"><span>Results</span><strong data-adminui-count>${engine.search(f.q,f).length}</strong></div></div></div></section><section data-adminui-results>${renderAdminUiResults(engine,f)}</section></div>`;
}
export function renderAdminUiDetail(engine,item){const x=engine.get(item.id);if(!x)return null;if(item.type==="admin-ui-control")return simple(x,"Admin control");if(item.type==="admin-ui-panel")return simple(x,"Admin panel");if(item.type==="admin-ui-schema")return simple(x,"Support schema");return profile(x);}
function simple(x,label){return `${header(x.name||x.label,x.description,`<div class="header-actions"><span class="status-badge status-badge--stable">${e(label)}</span><button class="button button--secondary button--compact" data-copy-value="${e(x.$id||x.id)}" data-copy-label="ID copied">${icon("fa-copy")} Copy ID</button></div>`)}<div class="stack stack--lg"><article class="paper-card"><div class="paper-card__header"><div><h2>Definition</h2></div></div><div class="paper-card__body"><pre class="code-block registry-raw"><code>${e(JSON.stringify(x,null,2))}</code></pre></div></article></div>`;}
function profile(p){return `${header(p.name,p.description,`<div class="header-actions"><span class="status-badge status-badge--stable">Internal only</span><span class="tag">${e(p.screenKind)}</span><button class="button button--secondary button--compact" data-copy-value="${e(p.$id)}" data-copy-label="Profile ID copied">${icon("fa-copy")} Copy ID</button></div>`)}<div class="stack stack--lg"><section class="manifest-detail-grid"><div><span>Resource</span><code>${e(p.resource)}</code></div><div><span>Route</span><code>${e(p.route)}</code></div><div><span>Context</span><strong>${e(p.context?.kind)}</strong></div><div><span>Layout</span><strong>${e(p.layoutTemplate)}</strong></div><div><span>Customer visible</span><strong>No</strong></div><div><span>Permissions</span><strong>${p.permissions?.length??0}</strong></div></section>${p.baseCustomerProfileId?`<section class="paper-card"><div class="paper-card__header"><div><h2>Customer CMS presentation reference</h2><p>Admin reuses customer resource presentation by reference instead of creating divergent field schemas.</p></div></div><div class="paper-card__body">${chips([p.baseCustomerProfileId])}</div></section>`:""}<div class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Canonical targets</h2></div></div><div class="paper-card__body">${chips(p.targetContracts)}</div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Required permissions</h2></div></div><div class="paper-card__body">${chips(p.permissions)}</div></article></div><article class="paper-card"><div class="paper-card__header"><div><h2>Internal panels</h2><p>Customer CMS never receives these platform-only context surfaces.</p></div></div><div class="paper-card__body"><div class="adminui-panel-grid">${(p.panels??[]).map(x=>`<a href="${link('adminUi.panel.'+x)}"><strong>${e(x)}</strong></a>`).join("")}</div></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Privileged actions</h2><p>Every action is reauthorized server-side. Admin status never bypasses canonical business rules.</p></div></div><div class="paper-card__body cmsui-table-wrap"><table class="data-table"><thead><tr><th>Action</th><th>Permission</th><th>Risk</th><th>Recent auth</th><th>Confirm</th></tr></thead><tbody>${(p.actions??[]).map(a=>`<tr><td><strong>${e(a.actionId)}</strong></td><td><a href="${link(a.permissionId)}"><code>${e(a.permissionId)}</code></a></td><td>${e(a.riskLevel)}</td><td>${a.requiresRecentAuthentication?"Required":"No"}</td><td>${a.requiresExplicitConfirmation||a.riskLevel==="destructive"||a.riskLevel==="privileged"?"Required/Policy":"No"}</td></tr>`).join("")||'<tr><td colspan="5">No direct privileged actions.</td></tr>'}</tbody></table></div></article><div class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Security boundary</h2></div></div><div class="paper-card__body"><dl class="data-list"><div class="data-list__row"><dt>Server authorization</dt><dd>Required</dd></div><div class="data-list__row"><dt>Direct URL recheck</dt><dd>Required</dd></div><div class="data-list__row"><dt>Secret values rendered</dt><dd>Never</dd></div><div class="data-list__row"><dt>Customer impersonation</dt><dd>Disabled</dd></div><div class="data-list__row"><dt>Business-rule bypass</dt><dd>Never</dd></div></dl></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Notes</h2></div></div><div class="paper-card__body"><ul class="list-clean">${(p.notes??[]).map(n=>`<li class="list-item"><span class="list-item__icon">${icon("fa-circle-info")}</span><div>${e(n)}</div></li>`).join("")||'<li class="empty-note">No profile-specific notes.</li>'}</ul></div></article></div><article class="paper-card"><div class="paper-card__header"><div><h2>Authoritative Admin profile JSON</h2></div><button class="button button--ghost button--compact" data-copy-value="${e(JSON.stringify(p))}" data-copy-label="Profile JSON copied">${icon("fa-copy")} Copy JSON</button></div><div class="paper-card__body"><pre class="code-block registry-raw"><code>${e(JSON.stringify(p,null,2))}</code></pre></div></article></div>`;}
