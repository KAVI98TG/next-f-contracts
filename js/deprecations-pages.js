const h=(v)=>String(v??'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
const icon=(n)=>`<i class="fa-solid ${n}" aria-hidden="true"></i>`;
const tri=(v)=>v===true?'Required':v===false?'Not required':'Unknown';
const tagList=(a=[])=>a.length?`<div class="tag-cloud">${a.map(x=>`<span class="tag">${h(x)}</span>`).join('')}</div>`:'<span class="muted">None recorded.</span>';
const statusBadge=(s)=>`<span class="status-badge ${s==='removed'?'status-badge--danger':'status-badge--draft'}">${h(s==='removed'?'Removed':'Deprecated')}</span>`;
function header(engine){return `<header class="page-header"><div><span class="eyebrow">Lifecycle · Phase 28</span><h1>Deprecations</h1><p>Operational lifecycle records, replacements, support windows and migration guidance. Deprecated definitions remain discoverable; Sites never silently upgrade.</p></div><div class="header-actions"><a class="button button--secondary" href="#/lifecycle/changelog">${icon('fa-clock-rotate-left')} Changelog</a><a class="button button--secondary" href="#/lifecycle/diff">${icon('fa-code-compare')} Contract Diff</a></div></header>`;}
function tabs(view){return `<div class="deprecation-tabs" role="navigation" aria-label="Deprecation views"><a class="deprecation-tab ${view==='overview'?'is-active':''}" href="#/lifecycle/deprecations">Overview</a><a class="deprecation-tab ${view==='records'?'is-active':''}" href="#/lifecycle/deprecations?view=records">Records</a><a class="deprecation-tab ${view==='definitions'?'is-active':''}" href="#/lifecycle/deprecations?view=definitions">Definitions</a></div>`;}
function overview(engine){const i=engine.index;return `<div class="stack stack--lg"><div class="deprecation-metrics"><article class="paper-card"><span>${icon('fa-triangle-exclamation')}</span><div><small>Active deprecations</small><strong>${i.activeDeprecationCount??0}</strong><p>Definitions still discoverable for compatibility.</p></div></article><article class="paper-card"><span>${icon('fa-calendar-xmark')}</span><div><small>Upcoming removals</small><strong>${i.upcomingRemovalCount??0}</strong><p>Only authoritative removal targets are counted.</p></div></article><article class="paper-card"><span>${icon('fa-box-archive')}</span><div><small>Removed</small><strong>${i.removedCount??0}</strong><p>Historical lifecycle records retained.</p></div></article><article class="paper-card"><span>${icon('fa-diagram-project')}</span><div><small>Lifecycle states</small><strong>${engine.lifecycleStates.states?.length??0}</strong><p>One canonical lifecycle vocabulary.</p></div></article></div><article class="deprecation-callout"><span>${icon('fa-circle-check')}</span><div><h2>${(i.recordCount??0)===0?'No canonical deprecations are active':'Deprecation records are active'}</h2><p>${(i.recordCount??0)===0?'The V0.29.0 Registry contains no canonical items marked deprecated or removed. The empty record set is intentional and evidence-based; no fake lifecycle history has been created.':'Use the records view to inspect replacement, support and migration requirements.'}</p></div></article><div class="deprecation-two-col"><article class="paper-card"><div class="paper-card__header"><div><h2>Lifecycle policy</h2><p>Stable machine rules applied to every future deprecation.</p></div></div><div class="paper-card__body"><ol class="compat-steps"><li>New work prefers a canonical replacement when one exists.</li><li>Existing Sites stay pinned until explicit migration and validation.</li><li>Known support/removal dates require authoritative evidence.</li><li>Removed definitions remain historically discoverable.</li><li>Unknown replacement/support data remains unknown.</li></ol></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Canonical lifecycle</h2><p>Do not invent parallel state vocabulary in individual domains.</p></div></div><div class="paper-card__body"><div class="deprecation-lifecycle">${(engine.lifecycleStates.states??[]).map(s=>`<div><strong>${h(s.label)}</strong><code>${h(s.id)}</code><p>${h(s.description)}</p></div>`).join('')}</div></div></article></div><article class="paper-card"><div class="paper-card__header"><div><h2>Affected Sites</h2><p>Future Admin integration point.</p></div></div><div class="paper-card__body"><div class="registry-inline-empty">${icon('fa-circle-info')} Live Site compatibility telemetry is not implemented in the Contract Registry. Affected Site counts remain unavailable until a future runtime/Admin integration provides authoritative evidence.</div></div></article></div>`;}
function filters(engine,f){const o=(vals,v)=>vals.map(x=>`<option value="${h(x)}" ${x===v?'selected':''}>${h(x)}</option>`).join('');return `<section class="paper-card deprecation-filter-card"><div class="deprecation-filters"><label class="deprecation-filter-wide"><span>Search</span><input type="search" value="${h(f.q)}" placeholder="ID, contract, reason, replacement…" data-deprecation-q></label><label><span>Status</span><select data-deprecation-status><option value="">All statuses</option>${o(['deprecated','removed'],f.status)}</select></label><label><span>Domain</span><select data-deprecation-domain><option value="">All domains</option>${o(engine.index.domains??[],f.domain)}</select></label><label><span>Type</span><select data-deprecation-type><option value="">All types</option>${o(engine.index.types??[],f.type)}</select></label><label><span>Severity</span><select data-deprecation-severity><option value="">All severities</option>${o((engine.severityLevels.levels??[]).map(x=>x.id),f.severity)}</select></label><label><span>Replacement</span><select data-deprecation-replacement><option value="">Any replacement state</option>${o(['replacement','no-replacement','unknown'],f.replacement)}</select></label><label><span>Module</span><select data-deprecation-module><option value="">All modules</option>${o(engine.index.modules??[],f.module)}</select></label><label class="deprecation-check"><input type="checkbox" ${f.migration?'checked':''} data-deprecation-migration><span>Migration needed</span></label><button class="button button--secondary" type="button" data-deprecation-clear>Clear</button></div></section>`;}
function recordCard(r){return `<article class="paper-card deprecation-record"><div class="deprecation-record__head"><div><div class="deprecation-record__meta">${statusBadge(r.status)}<span class="tag">${h(r.severity)}</span><span class="tag">${h(r.affectedDomain??'unknown domain')}</span></div><h3><a href="#/lifecycle/deprecations?view=record&id=${encodeURIComponent(r.deprecationId)}">${h(r.affectedName??r.affectedRegistryId)}</a></h3><code>${h(r.affectedRegistryId)}</code><p>${h(r.reason)}</p></div><a class="button button--secondary button--compact" href="#/lifecycle/deprecations?view=record&id=${encodeURIComponent(r.deprecationId)}">Inspect</a></div></article>`;}
function records(engine,f){const rows=engine.search(f);return `<div class="stack">${filters(engine,f)}<div class="changelog-result-head"><span><strong data-deprecation-count>${rows.length}</strong> records</span></div><div class="deprecation-record-list" data-deprecation-results>${rows.length?rows.map(recordCard).join(''):`<article class="paper-card"><div class="paper-card__body"><div class="empty-state"><h2>No deprecation records match</h2><p>${engine.records.length?'Adjust the filters.':'The canonical Registry currently has no deprecated or removed items. This empty state is authoritative.'}</p></div></div></article>`}</div></div>`;}
function recordDetail(engine,r){if(!r)return `<article class="paper-card"><div class="paper-card__body"><div class="empty-state"><h2>Deprecation record not found</h2><p>The requested record ID does not exist.</p></div></div></article>`;const rep=r.replacement??{};return `<div class="stack stack--lg"><div class="deprecation-detail-head"><div><a class="back-link" href="#/lifecycle/deprecations?view=records">${icon('fa-arrow-left')} All deprecations</a><div class="deprecation-record__meta">${statusBadge(r.status)}<span class="tag">${h(r.severity)}</span></div><h2>${h(r.affectedName??r.affectedRegistryId)}</h2><code>${h(r.deprecationId)}</code><p>${h(r.reason)}</p></div></div><div class="deprecation-two-col"><article class="paper-card"><div class="paper-card__header"><div><h3>Lifecycle</h3></div></div><div class="paper-card__body"><dl class="data-list"><div class="data-list__row"><dt>Affected definition</dt><dd><a href="#/registry/item/${encodeURIComponent(r.affectedRegistryId)}"><code>${h(r.affectedRegistryId)}</code></a></dd></div><div class="data-list__row"><dt>Deprecated since</dt><dd>V${h(r.deprecatedSince)}</dd></div><div class="data-list__row"><dt>Version range</dt><dd>${h(r.affectedVersionRange)}</dd></div><div class="data-list__row"><dt>Last supported</dt><dd>${h(r.lastSupportedVersion??'Unknown')}</dd></div><div class="data-list__row"><dt>Support until</dt><dd>${h(r.supportUntil??'Unknown')}</dd></div><div class="data-list__row"><dt>Removal target</dt><dd>${h(r.breakingRemovalTarget??'Not established')}</dd></div></dl></div></article><article class="paper-card"><div class="paper-card__header"><div><h3>Replacement</h3></div></div><div class="paper-card__body"><dl class="data-list"><div class="data-list__row"><dt>Disposition</dt><dd>${h(rep.disposition??'unknown')}</dd></div><div class="data-list__row"><dt>Replacement ID</dt><dd>${rep.replacementId?`<a href="#/registry/item/${encodeURIComponent(rep.replacementId)}"><code>${h(rep.replacementId)}</code></a>`:'None / unknown'}</dd></div><div class="data-list__row"><dt>Instructions</dt><dd>${h(rep.instructions??'No authoritative instructions recorded.')}</dd></div><div class="data-list__row"><dt>Migration guide</dt><dd>${h(rep.migrationGuideReference??'Unavailable')}</dd></div></dl></div></article></div><article class="paper-card"><div class="paper-card__header"><div><h3>Migration impact</h3></div></div><div class="paper-card__body"><div class="deprecation-migration-grid"><div><span>Data</span><strong>${tri(r.dataMigrationRequired)}</strong></div><div><span>Code</span><strong>${tri(r.codeMigrationRequired)}</strong></div><div><span>Configuration</span><strong>${tri(r.configurationMigrationRequired)}</strong></div><div><span>Auto-migratable</span><strong>${r.autoMigratable===true?'Yes':r.autoMigratable===false?'No':'Unknown'}</strong></div><div><span>Manual review</span><strong>${r.manualReviewRequired===true?'Required':r.manualReviewRequired===false?'Not required':'Unknown'}</strong></div></div></div></article><div class="deprecation-two-col"><article class="paper-card"><div class="paper-card__header"><div><h3>Affected modules</h3></div></div><div class="paper-card__body">${tagList(r.modulesAffected)}</div></article><article class="paper-card"><div class="paper-card__header"><div><h3>Affected APIs</h3></div></div><div class="paper-card__body">${tagList(r.apisAffected)}</div></article></div><article class="paper-card"><div class="paper-card__header"><div><h3>Evidence and lifecycle links</h3></div></div><div class="paper-card__body"><dl class="data-list"><div class="data-list__row"><dt>Changelog entries</dt><dd>${tagList(r.relatedChangelogEntries)}</dd></div><div class="data-list__row"><dt>Diff evidence</dt><dd>${tagList(r.relatedDiffEvidence)}</dd></div><div class="data-list__row"><dt>Source</dt><dd><code>${h(r.sourceReference)}</code></dd></div></dl></div></article><article class="paper-card"><div class="paper-card__header"><div><h3>Raw machine-readable JSON</h3></div><button class="button button--ghost button--compact" type="button" data-copy-value="${h(JSON.stringify(r))}" data-copy-label="Deprecation JSON copied">${icon('fa-copy')} Copy JSON</button></div><div class="paper-card__body"><pre class="code-block registry-raw"><code>${h(JSON.stringify(r,null,2))}</code></pre></div></article></div>`;}
function definitions(engine){return `<div class="stack stack--lg"><article class="deprecation-callout"><span>${icon('fa-diagram-project')}</span><div><h2>Lifecycle contracts</h2><p>These schemas make deprecation, replacement and support-window behavior machine-readable for humans, Codex and future migration tooling.</p></div></article><div class="changelog-definition-grid">${engine.definitions.map(d=>`<article class="paper-card changelog-definition"><div><span class="tag">Schema</span><h3>${h(d.name)}</h3><code>${h(d.$id)}</code><p>${h(d.description)}</p></div><a class="button button--secondary button--compact" href="#/registry/item/${encodeURIComponent(d.$id)}">Inspect contract</a></article>`).join('')}</div></div>`;}
export function renderDeprecationsPage(engine,f){const view=f.view??'overview';let body;if(view==='records')body=records(engine,f);else if(view==='record')body=recordDetail(engine,engine.get(f.id));else if(view==='definitions')body=definitions(engine);else body=overview(engine);return `${header(engine)}${tabs(view)}${body}`;}
export function renderDeprecationResults(engine,f){return engine.search(f).map(recordCard).join('')||`<article class="paper-card"><div class="paper-card__body"><div class="empty-state"><h2>No deprecation records match</h2><p>${engine.records.length?'Adjust the filters.':'The canonical Registry currently has no deprecated or removed items.'}</p></div></div></article>`;}
export function renderRegistryDeprecationCard(engine,registryId){const r=engine.forRegistryId(registryId);if(!r)return '' ;return `<article class="paper-card" data-registry-deprecation><div class="paper-card__header"><div><h2>Deprecation</h2><p>This Registry definition is governed by an active lifecycle record.</p></div>${statusBadge(r.status)}</div><div class="paper-card__body"><p>${h(r.reason)}</p><a class="button button--secondary button--compact" href="#/lifecycle/deprecations?view=record&id=${encodeURIComponent(r.deprecationId)}">Review migration path</a></div></article>`;}
