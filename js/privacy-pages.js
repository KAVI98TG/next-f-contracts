const h=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const icon=n=>`<i class="fa-solid ${n}" aria-hidden="true"></i>`;
const tags=a=>(a??[]).length?`<div class="privacy-tags">${a.map(x=>`<span class="tag">${h(x)}</span>`).join('')}</div>`:'<span class="muted">None</span>';
const regLink=id=>`<a href="#/registry/item/${encodeURIComponent(id)}"><code>${h(id)}</code></a>`;
const eligible=v=>`<span class="privacy-flow privacy-flow--${h(v)}">${h(v)}</span>`;
function header(e){return `<header class="page-header"><div class="page-header__copy"><p class="page-header__eyebrow">Standards</p><h1>Privacy & Data</h1><p>Classification, minimization, flow eligibility, retention, consent relevance, redaction and future-safe privacy operations.</p></div><div class="page-header__actions"><span class="status-badge status-badge--stable">Phase 30</span><span class="tag">${e.fieldHandling.length} field rules</span></div></header><div class="privacy-source" role="status">${icon('fa-user-shield')} <span>Runtime source</span><strong>${e.source==='authoritative-json'?'Authoritative privacy JSON':'Generated local fallback'}</strong><span class="tag">v${h(e.data.registryVersion)}</span></div>`;}
function tabs(v){const rows=[['overview','Overview'],['fields','Field classifications'],['retention','Retention'],['consent','Consent boundaries'],['coverage','High-risk coverage'],['operations','Operations'],['definitions','Definitions']];return `<nav class="privacy-tabs" aria-label="Privacy views">${rows.map(([id,l])=>`<a class="${v===id?'is-active':''}" href="#/standards/privacy?view=${id}">${h(l)}</a>`).join('')}</nav>`;}
function classBadge(c){return `<span class="privacy-class privacy-class--${h(c)}">${h(c)}</span>`;}
function overview(e){const counts=Object.fromEntries(e.classifications.map(c=>[c.id,e.fieldHandling.filter(x=>x.classification===c.id).length]));return `<div class="stack stack--lg"><section class="dashboard-grid dashboard-grid--5">${e.classifications.map(c=>`<article class="paper-card metric-card"><div class="metric-card__label">${h(c.label)}</div><div class="metric-card__value">${counts[c.id]??0}</div><p class="metric-card__meta">Explicit field handling records</p></article>`).join('')}</section><div class="callout"><span class="callout__icon">${icon('fa-circle-info')}</span><div><h3>Primary class + qualifiers</h3><p>Every Phase 30 handling record has one primary class. Financial, authentication, tracking, content and system metadata remain qualifiers, not competing sensitivity labels.</p></div></div><section><div class="section-header"><div><h2>Classification explorer</h2><p>Open field rules by primary sensitivity class.</p></div></div><div class="privacy-class-grid">${e.classifications.map(c=>`<a class="paper-card privacy-class-card" href="#/standards/privacy?view=fields&classification=${encodeURIComponent(c.id)}"><div>${classBadge(c.id)}<h3>${h(c.label)}</h3><p>${h(c.description)}</p></div><strong>${counts[c.id]??0} fields</strong></a>`).join('')}</div></section><section class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Data-flow rules</h2></div></div><div class="paper-card__body"><ul class="list-clean">${['Secrets are never public or analytics eligible.','Raw form payloads are not analytics payloads.','Logs minimize or redact personal/sensitive data.','Webhook/Event eligibility never overrides their canonical payload contracts.','Hashed or pseudonymized personal data is not automatically anonymous.'].map(x=>`<li class="list-item"><span class="list-item__icon">${icon('fa-check')}</span><div>${h(x)}</div></li>`).join('')}</ul></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Consent boundary</h2></div></div><div class="paper-card__body"><p>Business-purpose/form consent or acknowledgement and optional analytics/marketing consent are separate concepts. One cannot silently grant the other.</p><a class="button button--secondary button--compact" href="#/standards/privacy?view=consent">Review consent rules</a></div></article></section></div>`;}
function fieldCard(r){return `<article class="paper-card privacy-field-card"><div class="privacy-field-card__top"><div><span class="privacy-id">${h(r.handlingId)}</span><h3>${h(r.targetId)}<span class="muted">.${h(r.fieldPath)}</span></h3></div>${classBadge(r.classification)}</div><p>${h(r.purpose)}</p>${tags(r.qualifiers)}<div class="privacy-mini-grid"><div><span>Retention</span><strong>${h(r.retentionClass)}</strong></div><div><span>Consent</span><strong>${h(r.consentRelevance)}</strong></div><div><span>Redaction</span><strong>${h(r.redactionBehavior)}</strong></div><div><span>Export</span><strong>${h(r.exportSensitivity)}</strong></div></div><a class="button button--secondary button--compact" href="#/standards/privacy?view=field&id=${encodeURIComponent(r.handlingId)}">Inspect handling</a></article>`;}
function fields(e,f){const rows=e.search(f);const opts=(a,s,label=x=>x)=>a.map(x=>`<option value="${h(typeof x==='string'?x:x.id)}" ${(typeof x==='string'?x:x.id)===s?'selected':''}>${h(label(x))}</option>`).join('');return `<div class="stack stack--lg"><section class="paper-card"><div class="paper-card__header"><div><h2>Field-level classification search</h2><p><strong data-privacy-count>${rows.length}</strong> matching records</p></div><button class="button button--ghost button--compact" type="button" data-privacy-clear>${icon('fa-rotate-left')} Clear</button></div><div class="paper-card__body privacy-filter-grid"><label class="field field--search"><span>Search</span><input type="search" value="${h(f.q)}" placeholder="Target ID, field path, purpose..." data-privacy-q></label><label class="field"><span>Primary class</span><select data-privacy-classification><option value="">All classes</option>${opts(e.classifications,f.classification,x=>x.label)}</select></label><label class="field"><span>Qualifier</span><select data-privacy-qualifier><option value="">All qualifiers</option>${opts(e.qualifiers,f.qualifier,x=>x.label)}</select></label><label class="field"><span>Retention</span><select data-privacy-retention><option value="">All retention classes</option>${opts(e.retentionClasses,f.retention,x=>x.label)}</select></label><label class="field"><span>Consent relevance</span><select data-privacy-consent><option value="">All</option>${opts(e.consentRelevance,f.consent,x=>x.label)}</select></label><label class="field"><span>Public delivery</span><select data-privacy-public><option value="">Any</option><option value="eligible" ${f.public==='eligible'?'selected':''}>Eligible</option><option value="not-eligible" ${f.public==='not-eligible'?'selected':''}>Not eligible</option></select></label><label class="field"><span>Redaction</span><select data-privacy-redaction><option value="">All redaction behaviors</option>${opts(e.redactionBehaviors,f.redaction,x=>x.label)}</select></label><label class="field"><span>Flow</span><select data-privacy-flow><option value="">Any flow</option>${opts(['log','event','webhook','analytics'],f.flow)}</select></label><label class="field"><span>Flow eligibility</span><select data-privacy-flow-value><option value="">Any eligibility</option>${opts(e.eligibilityValues,f.flowValue,x=>x.label)}</select></label></div></section><div class="privacy-field-grid" data-privacy-results>${rows.map(fieldCard).join('')||'<article class="paper-card"><div class="paper-card__body"><div class="empty-state"><h2>No privacy records match</h2><p>Adjust the filters.</p></div></div></article>'}</div></div>`;}
function detail(e,r){if(!r)return `<article class="paper-card"><div class="paper-card__body"><div class="empty-state"><h2>Privacy handling record not found</h2></div></div></article>`;return `<div class="stack stack--lg"><a class="back-link" href="#/standards/privacy?view=fields">${icon('fa-arrow-left')} All field handling</a><section class="paper-card"><div class="paper-card__body"><div class="privacy-field-card__top"><div><span class="privacy-id">${h(r.handlingId)}</span><h2>${h(r.targetId)}.${h(r.fieldPath)}</h2></div>${classBadge(r.classification)}</div><p class="privacy-purpose">${h(r.purpose)}</p>${tags(r.qualifiers)}<p>${regLink(r.targetId)}</p></div></section><div class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h3>Flow eligibility</h3></div></div><div class="paper-card__body privacy-flow-list"><div><span>Public delivery</span><strong>${r.publicDeliveryEligible?'eligible':'not eligible'}</strong></div><div><span>Logs</span>${eligible(r.logEligibility)}</div><div><span>Events</span>${eligible(r.eventEligibility)}</div><div><span>Webhooks</span>${eligible(r.webhookEligibility)}</div><div><span>Analytics</span>${eligible(r.analyticsEligibility)}</div></div></article><article class="paper-card"><div class="paper-card__header"><div><h3>Lifecycle handling</h3></div></div><div class="paper-card__body privacy-flow-list"><div><span>Retention</span><strong>${h(r.retentionClass)}</strong></div><div><span>Export</span><strong>${h(r.exportSensitivity)}</strong></div><div><span>Consent relevance</span><strong>${h(r.consentRelevance)}</strong></div><div><span>Deletion</span><strong>${h(r.deletionBehavior)}</strong></div><div><span>Redaction</span><strong>${h(r.redactionBehavior)}</strong></div></div></article></div><article class="paper-card"><div class="paper-card__header"><div><h3>Related Security controls</h3></div></div><div class="paper-card__body">${tags(r.relatedSecurityControls)}</div></article><article class="paper-card"><div class="paper-card__header"><div><h3>Raw machine-readable JSON</h3></div><button class="button button--secondary button--compact" type="button" data-copy-value="${h(JSON.stringify(r,null,2))}" data-copy-label="Privacy JSON copied">${icon('fa-copy')} Copy</button></div><div class="paper-card__body"><pre class="code-block"><code>${h(JSON.stringify(r,null,2))}</code></pre></div></article></div>`;}
function retentionView(e){return `<div class="stack stack--lg"><div class="callout"><span class="callout__icon">${icon('fa-clock')}</span><div><h3>No fabricated legal durations</h3><p>Retention classes define policy buckets. Actual durations remain customer/provider/operational/legal policy decisions where authoritative.</p></div></div><div class="privacy-retention-grid">${e.retentionClasses.map(x=>`<article class="paper-card"><div class="paper-card__body"><span class="privacy-id">${h(x.id)}</span><h3>${h(x.label)}</h3><p>${h(x.description)}</p><strong>${e.fieldHandling.filter(r=>r.retentionClass===x.id).length} explicit fields</strong></div></article>`).join('')}</div></div>`;}
function consentView(e){return `<div class="stack stack--lg"><section class="paper-card"><div class="paper-card__header"><div><h2>Consent and purpose boundaries</h2><p>These rules cross-link Forms/Leads and Marketing consent without collapsing them.</p></div></div><div class="paper-card__body"><div class="privacy-rule-list">${e.consentBoundaries.map(x=>`<article><span class="privacy-id">${h(x.id)}</span><p>${h(x.statement)}</p></article>`).join('')}</div></div></section><section class="privacy-retention-grid">${e.consentRelevance.map(x=>`<article class="paper-card"><div class="paper-card__body"><h3>${h(x.label)}</h3><p>${h(x.description)}</p></div></article>`).join('')}</section></div>`;}
function coverageView(e){return `<div class="stack stack--lg"><div class="privacy-coverage-grid">${e.coverage.map(x=>`<article class="paper-card"><div class="paper-card__body"><div class="privacy-field-card__top"><h3>${h(x.area)}</h3><span class="status-badge status-badge--stable">${h(x.status)}</span></div><p>${h(x.notes)}</p><h4>Canonical targets</h4><div class="privacy-links">${(x.targetRegistryIds??[]).map(regLink).join('')}</div><p><strong>${(x.fieldHandlingIds??[]).length}</strong> field-level rules in this area.</p></div></article>`).join('')}</div></div>`;}
function operationsView(e){return `<div class="stack stack--lg"><div class="callout"><span class="callout__icon">${icon('fa-circle-info')}</span><div><h3>Defined primitives, not deployed workflow claims</h3><p>Phase 30 defines future-safe operations. <code>future-capability</code> and <code>contract-primitive</code> explicitly mean a complete runtime workflow is not claimed.</p></div></div><div class="privacy-operation-grid">${e.operations.map(x=>`<article class="paper-card"><div class="paper-card__body"><div class="privacy-field-card__top"><h3>${h(x.label)}</h3><span class="tag">${h(x.status)}</span></div><p>${h(x.description)}</p><p><strong>Runtime implemented:</strong> ${x.implementedRuntime?'yes':'no'}</p></div></article>`).join('')}</div></div>`;}
function definitions(e){return `<div class="privacy-definition-grid">${e.definitions.map(x=>`<article class="paper-card"><div class="paper-card__body"><span class="privacy-id">${h(x.$id)}</span><h3>${h(x.name)}</h3><p>${h(x.description)}</p><a href="#/registry/item/${encodeURIComponent(x.$id)}">Registry definition</a></div></article>`).join('')}</div>`;}
export function renderPrivacyResults(e,f){return e.search(f).map(fieldCard).join('')||'<article class="paper-card"><div class="paper-card__body"><div class="empty-state"><h2>No privacy records match</h2></div></div></article>';}
export function renderPrivacyPage(e,f={}){const view=f.view||'overview';let body=overview(e);if(view==='fields')body=fields(e,f);else if(view==='field')body=detail(e,e.get(f.id));else if(view==='retention')body=retentionView(e);else if(view==='consent')body=consentView(e);else if(view==='coverage')body=coverageView(e);else if(view==='operations')body=operationsView(e);else if(view==='definitions')body=definitions(e);return `${header(e)}${tabs(view)}${body}`;}
