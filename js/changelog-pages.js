const h=(v)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const icon=(n)=>`<i class="fa-solid ${n}" aria-hidden="true"></i>`;
const catIcon={added:'fa-plus',changed:'fa-pen-to-square',fixed:'fa-wrench',security:'fa-shield-halved',deprecated:'fa-triangle-exclamation',removed:'fa-trash-can',breaking:'fa-burst',migration:'fa-route',integrity:'fa-shield',documentation:'fa-book',preserved:'fa-lock',internal:'fa-gear'};
function catBadge(engine,id){const c=engine.category(id);return `<span class="changelog-cat changelog-cat--${h(id)}">${icon(catIcon[id]??'fa-circle')} ${h(c?.label??id)}</span>`;}
function evidenceBadge(engine,id){const e=engine.evidence(id);return `<span class="changelog-evidence changelog-evidence--${h(id)}">${icon(id==='notes-and-exact-registry'?'fa-circle-check':id==='release-notes-only'?'fa-file-lines':'fa-circle-question')} ${h(e?.label??id)}</span>`;}
function pageHeader(engine){return `<div class="page-header"><div><span class="page-eyebrow">Lifecycle</span><h1>Changelog</h1><p>Authoritative release history with explicit provenance, change impact, affected contracts and links to exact Diff and Compatibility evidence.</p></div><div class="header-actions"><span class="status-badge status-badge--stable">v${h(engine.currentVersion)}</span><a class="button button--secondary button--compact" href="./standards/35-changelog-standard.md">${icon('fa-file-lines')} Standard</a></div></div>`;}
function tabs(view){const current=view==='change'?'entries':view;const rows=[['overview','Overview','fa-grid-2'],['releases','Releases','fa-clock-rotate-left'],['entries','Change entries','fa-list'],['definitions','Definitions','fa-database']];return `<nav class="changelog-tabs" aria-label="Changelog views">${rows.map(([id,label,ic])=>`<a class="changelog-tab ${current===id?'is-active':''}" href="#/lifecycle/changelog${id==='overview'?'':`?view=${id}`}" ${current===id?'aria-current="page"':''}>${icon(ic)} ${label}</a>`).join('')}</nav>`;}
function metric(label,value,caption,ic){return `<article class="paper-card changelog-metric"><span>${icon(ic)}</span><div><small>${h(label)}</small><strong>${h(value)}</strong><p>${h(caption)}</p></div></article>`;}
function tri(v){return v===true?'Required':v===false?'Not required':'Unknown';}
function ynu(v){return v===true?'Yes':v===false?'No':'Unknown';}
function versionLinks(versions=[]){return versions.length?versions.map(v=>`<a class="tag" href="#/lifecycle/changelog?view=releases&release=${encodeURIComponent(v)}">V${h(v)}</a>`).join(' '):'<span class="muted">None recorded</span>';}
function affectedLinks(ids=[]){return ids.length?`<div class="changelog-affected">${ids.map(id=>`<a href="#/registry/item/${encodeURIComponent(id)}"><code>${h(id)}</code></a>`).join('')}</div>`:'<span class="muted">No explicit Registry IDs recorded.</span>';}
function completionBadge(r){return r.recordCompleteness==='incomplete'?`<span class="status-badge status-badge--draft">${icon('fa-triangle-exclamation')} Incomplete history</span>`:`<span class="status-badge status-badge--stable">${icon('fa-circle-check')} Complete record</span>`;}
function releaseCard(engine,r){const cats=Object.entries(r.categoryCounts??{});return `<article class="paper-card changelog-release-card"><div class="changelog-release-card__head"><div><div class="changelog-release-card__version"><strong>V${h(r.version)}</strong><span>Phase ${h(r.phase)}</span></div><h3>${h(r.title)}</h3><p>${h(r.summary)}</p></div><div class="changelog-release-card__badges">${evidenceBadge(engine,r.evidence?.evidenceLevel)}${completionBadge(r)}</div></div><div class="changelog-release-card__meta"><span>${icon('fa-list')} ${h(r.entryCount)} entries</span><span>${icon('fa-calendar')} ${r.releaseDate?h(r.releaseDate):'Date unavailable'}</span><span>${icon('fa-code-branch')} ${r.sourceVersion?`V${h(r.sourceVersion)} → V${h(r.targetVersion??r.version)}`:`Initial recorded release → V${h(r.version)}`}</span><span>${icon('fa-triangle-exclamation')} Breaking: ${ynu(r.breakingChange)}</span><span>${icon('fa-route')} Migration: ${tri(r.migrationRequired)}</span></div><div class="changelog-release-card__cats">${cats.slice(0,12).map(([id,count])=>`${catBadge(engine,id)}<span class="changelog-count">${count}</span>`).join('')}</div><div class="changelog-release-card__actions"><a class="button button--secondary button--compact" href="#/lifecycle/changelog?view=releases&release=${encodeURIComponent(r.version)}">${icon('fa-eye')} View release</a>${r.evidence?.diffAvailable?`<a class="button button--ghost button--compact" href="${h(r.evidence.diffRoute)}">${icon('fa-code-compare')} Diff from previous</a>`:''}</div></article>`;}
function overview(engine){const s=engine.releaseStats(),current=engine.release(engine.currentVersion);return `<div class="stack stack--lg"><section class="changelog-callout"><span>${icon('fa-clock-rotate-left')}</span><div><h2>Narrative history + exact evidence</h2><p>Release notes explain intent. Contract Diff proves exact structural change when snapshots exist. Compatibility Center remains the authority for adoption/support decisions.</p></div></section><div class="changelog-metrics">${metric('Current release',`V${engine.currentVersion}`,'Coordinated development train','fa-code-branch')}${metric('Release notes',s.releaseCount,'Machine-readable release records','fa-tags')}${metric('Change entries',s.entryCount,'Stable searchable change IDs','fa-list')}${metric('Incomplete history',s.incompleteCount,'Explicitly marked, never reconstructed','fa-box-archive')}</div><div class="changelog-two-col"><article class="paper-card"><div class="paper-card__header"><div><h2>Current release</h2><p>Latest authoritative narrative record.</p></div>${evidenceBadge(engine,current.evidence.evidenceLevel)}</div><div class="paper-card__body">${releaseCard(engine,current)}</div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Evidence model</h2><p>Release-note confidence remains explicit.</p></div></div><div class="paper-card__body changelog-evidence-list">${engine.evidenceLevels.map(e=>`<div><strong>${h(e.label)}</strong><p>${h(e.description)}</p></div>`).join('')}</div></article></div><article class="paper-card"><div class="paper-card__header"><div><h2>Change categories</h2><p>Machine categories are stable; presentation labels remain separate.</p></div></div><div class="paper-card__body changelog-category-grid">${engine.sections.map(c=>`<div>${catBadge(engine,c.id)}<p>${h(c.description)}</p></div>`).join('')}</div></article><div class="changelog-two-col"><article class="paper-card"><div class="paper-card__header"><div><h2>Upgrade review</h2><p>Required evidence chain before a Site version pin changes.</p></div></div><div class="paper-card__body"><ol class="compat-steps"><li>Read the target Changelog.</li><li>Inspect exact Contract Diff when available.</li><li>Review Compatibility Center findings.</li><li>Resolve migration/deprecation guidance.</li><li>Validate the target Site declaration.</li><li>Upgrade the Manifest explicitly.</li></ol></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Historical integrity</h2><p>Unknown and unavailable evidence remain visible.</p></div></div><div class="paper-card__body"><div class="compat-note">${icon('fa-circle-info')} A missing exact Registry snapshot is never reconstructed. Unknown action/migration/breaking flags remain <strong>Unknown</strong> rather than defaulting to No.</div></div></article></div></div>`;}
function option(value,label,current){return `<option value="${h(value)}" ${String(current)===String(value)?'selected':''}>${h(label)}</option>`;}
function filters(engine,f){return `<div class="paper-card changelog-filter-card"><div class="changelog-filters"><label class="changelog-filter-wide"><span>Search</span><input type="search" value="${h(f.q)}" placeholder="Search releases, change IDs or affected contracts..." data-changelog-q></label><label><span>Version</span><select data-changelog-release><option value="">All versions</option>${engine.releases.map(r=>option(r.version,`V${r.version}`,f.release)).join('')}</select></label><label><span>Phase</span><select data-changelog-phase><option value="">All phases</option>${engine.phases.map(p=>option(String(p),`Phase ${p}`,f.phase)).join('')}</select></label><label><span>Change type</span><select data-changelog-category><option value="">All types</option>${engine.sections.map(c=>option(c.id,c.label,f.category)).join('')}</select></label><label><span>Domain</span><select data-changelog-domain><option value="">All domains</option>${engine.domains.map(v=>option(v,v,f.domain)).join('')}</select></label><label><span>Module</span><select data-changelog-module><option value="">All modules</option>${engine.modules.map(v=>option(v,v,f.module)).join('')}</select></label><label><span>Evidence</span><select data-changelog-evidence><option value="">All evidence</option>${engine.evidenceLevels.map(e=>option(e.id,e.label,f.evidence)).join('')}</select></label><label class="changelog-check"><input type="checkbox" data-changelog-breaking ${f.breaking?'checked':''}><span>Breaking only</span></label><label class="changelog-check"><input type="checkbox" data-changelog-migration ${f.migration?'checked':''}><span>Migration required</span></label><button class="button button--ghost button--compact" type="button" data-changelog-clear>${icon('fa-rotate-left')} Clear</button></div>${f.affected?`<div class="changelog-filter-context">${icon('fa-link')} Showing entries affecting <code>${h(f.affected)}</code> <a href="#/registry/item/${encodeURIComponent(f.affected)}">Open contract</a></div>`:''}</div>`;}
function releaseList(engine,f){const rows=engine.searchReleases(f);return `<div class="changelog-result-head"><span><strong data-changelog-count>${rows.length}</strong> releases</span></div><div class="changelog-release-list" data-changelog-results>${rows.length?rows.map(r=>releaseCard(engine,r)).join(''):`<div class="empty-state"><h3>No releases match</h3><p>Adjust the Changelog filters.</p></div>`}</div>`;}
function affectedSummary(r){const a=r.affected??{};const rows=[['Domains',a.domains],['Modules',a.modules],['Contracts / Registry IDs',a.contracts],['Events',a.events],['Permissions',a.permissions],['Webhooks',a.webhooks],['APIs',a.apis],['Manifests',a.manifests],['Customer CMS metadata',a.cmsMetadata],['NEXT F Admin metadata',a.adminMetadata]];return `<article class="paper-card"><div class="paper-card__header"><div><h3>Affected surfaces</h3><p>Derived only from explicit affected Registry IDs in this release.</p></div></div><div class="paper-card__body changelog-affected-groups">${rows.map(([label,ids])=>`<div><strong>${h(label)}</strong>${ids?.length?affectedLinks(ids):'<span class="muted">None explicitly recorded.</span>'}</div>`).join('')}</div></article>`;}
function releaseDetail(engine,r){if(!r)return `<div class="empty-state"><h2>Release not found</h2><p>The requested release note does not exist.</p></div>`;return `<div class="stack stack--lg"><div class="changelog-release-detail-head"><div><a class="back-link" href="#/lifecycle/changelog?view=releases">${icon('fa-arrow-left')} All releases</a><div class="changelog-release-card__version"><strong>V${h(r.version)}</strong><span>Phase ${h(r.phase)}</span></div><h2>${h(r.title)}</h2><p>${h(r.summary)}</p></div><div class="changelog-release-card__badges">${evidenceBadge(engine,r.evidence.evidenceLevel)}${completionBadge(r)}</div></div>${r.recordCompleteness==='incomplete'?`<div class="callout callout--warning">${icon('fa-triangle-exclamation')}<div><strong>Incomplete historical evidence</strong><p>${h(r.incompletenessReason)}</p></div></div>`:''}<div class="changelog-detail-grid"><article class="paper-card"><div class="paper-card__header"><div><h3>Release identity</h3></div></div><div class="paper-card__body"><dl class="data-list"><div class="data-list__row"><dt>Source version</dt><dd>${r.sourceVersion?`V${h(r.sourceVersion)}`:'Not applicable / unavailable'}</dd></div><div class="data-list__row"><dt>Target version</dt><dd>V${h(r.targetVersion??r.version)}</dd></div><div class="data-list__row"><dt>Release date</dt><dd>${r.releaseDate?h(r.releaseDate):'Unavailable'}</dd></div><div class="data-list__row"><dt>Record completeness</dt><dd>${h(r.recordCompleteness)}</dd></div><div class="data-list__row"><dt>Breaking change</dt><dd>${ynu(r.breakingChange)}</dd></div><div class="data-list__row"><dt>Migration required</dt><dd>${tri(r.migrationRequired)}</dd></div></dl></div></article><article class="paper-card"><div class="paper-card__header"><div><h3>Evidence & support</h3></div></div><div class="paper-card__body"><dl class="data-list"><div class="data-list__row"><dt>Evidence</dt><dd>${h(engine.evidence(r.evidence.evidenceLevel)?.label??r.evidence.evidenceLevel)}</dd></div><div class="data-list__row"><dt>Exact Registry snapshot</dt><dd>${r.provenance.exactRegistrySnapshotAvailable?'Available':'Unavailable'}</dd></div><div class="data-list__row"><dt>Support level</dt><dd>${h(String(r.support?.supportLevel??'unknown').replaceAll('-',' '))}</dd></div><div class="data-list__row"><dt>Compatibility</dt><dd>${h(String(r.support?.compatibilityStatus??'unknown').replaceAll('-',' '))}</dd></div><div class="data-list__row"><dt>Production eligible</dt><dd>${r.support?.productionEligible?'Yes':'No'}</dd></div></dl></div></article></div><article class="paper-card"><div class="paper-card__header"><div><h3>Evidence actions</h3></div></div><div class="paper-card__body changelog-actions-stack">${r.evidence.diffAvailable?`<a class="button button--secondary" href="${h(r.evidence.diffRoute)}">${icon('fa-code-compare')} Contract Diff from previous release</a>`:`<span class="compat-note">${icon('fa-circle-info')} Exact adjacent Diff is unavailable for this release.</span>`}<a class="button button--secondary" href="#/lifecycle/compatibility">${icon('fa-shield-halved')} Compatibility Center</a>${r.evidence.migrationRoute?`<a class="button button--secondary" href="${h(r.evidence.migrationRoute)}">${icon('fa-route')} Migration guidance</a>`:''}<a class="button button--secondary" href="./registry/changelog/releases/${h(r.version)}.json">${icon('fa-file-code')} Raw release JSON</a><button class="button button--ghost" type="button" data-copy-value="${h(JSON.stringify(r))}" data-copy-label="Release JSON copied">${icon('fa-copy')} Copy release JSON</button></div></article>${affectedSummary(r)}${r.sections.map(sec=>`<section class="paper-card changelog-section"><div class="paper-card__header"><div><h3>${h(sec.label)}</h3><p>${h(engine.category(sec.category)?.description??'')}</p></div>${catBadge(engine,sec.category)}</div><div class="paper-card__body"><ul class="changelog-entry-bullets">${sec.entries.map(e=>`<li id="${h(e.entryId)}"><div><a class="changelog-entry-title" href="#/lifecycle/changelog?view=change&entry=${encodeURIComponent(e.entryId)}"><strong>${h(e.title??e.summary)}</strong></a><p>${h(e.description??e.summary)}</p>${e.affectedRegistryIds?.length?affectedLinks(e.affectedRegistryIds):''}</div><a class="changelog-entry-id" href="#/lifecycle/changelog?view=change&entry=${encodeURIComponent(e.entryId)}"><code>${h(e.entryId)}</code></a></li>`).join('')}</ul></div></section>`).join('')}</div>`;}
function entryRow(engine,e){return `<article class="changelog-entry-row"><div><div class="changelog-entry-row__meta"><a href="#/lifecycle/changelog?view=releases&release=${encodeURIComponent(e.releaseVersion)}"><strong>V${h(e.releaseVersion)}</strong></a>${catBadge(engine,e.category)}${e.impact?.breakingChange===true?'<span class="status-badge status-badge--danger">Breaking</span>':''}${e.impact?.migrationRequired===true?'<span class="status-badge status-badge--draft">Migration</span>':''}</div><h3><a href="#/lifecycle/changelog?view=change&entry=${encodeURIComponent(e.entryId)}">${h(e.title??e.summary)}</a></h3><p>${h(e.description??e.summary)}</p>${e.affectedRegistryIds?.length?affectedLinks(e.affectedRegistryIds):''}</div><a class="changelog-entry-id" href="#/lifecycle/changelog?view=change&entry=${encodeURIComponent(e.entryId)}"><code>${h(e.entryId)}</code></a></article>`;}
function entries(engine,f){const rows=engine.searchEntries(f);return `<div class="stack">${filters(engine,f)}<div class="changelog-result-head"><span><strong data-changelog-count>${rows.length}</strong> entries</span></div><div class="paper-card"><div class="paper-card__body changelog-entry-list" data-changelog-results>${rows.length?rows.slice(0,500).map(e=>entryRow(engine,e)).join(''):`<div class="empty-state"><h3>No entries match</h3><p>Adjust the filters.</p></div>`}${rows.length>500?`<p class="relationship-limit-note">Showing the first 500 of ${rows.length} entries. Narrow the filters for a smaller review set.</p>`:''}</div></div></div>`;}
function actionTable(impact={}){const rows=[['Customer Site',impact.customerSiteActionRequired],['NEXT F Admin',impact.adminActionRequired],['NEXT F API',impact.apiActionRequired],['Customer CMS',impact.cmsActionRequired]];return `<div class="changelog-action-grid">${rows.map(([label,v])=>`<div><span>${h(label)}</span><strong>${tri(v)}</strong></div>`).join('')}</div>`;}
function changeDetail(engine,e){if(!e)return `<div class="empty-state"><h2>Change entry not found</h2><p>The requested stable change ID does not exist.</p></div>`;const r=engine.release(e.releaseVersion);return `<div class="stack stack--lg"><div class="changelog-release-detail-head"><div><a class="back-link" href="#/lifecycle/changelog?view=entries">${icon('fa-arrow-left')} All change entries</a><div class="changelog-entry-row__meta"><a href="#/lifecycle/changelog?view=releases&release=${encodeURIComponent(e.releaseVersion)}"><strong>V${h(e.releaseVersion)}</strong></a>${catBadge(engine,e.category)}</div><h2>${h(e.title??e.summary)}</h2><p>${h(e.description??e.summary)}</p><code>${h(e.entryId)}</code></div><div class="header-actions"><button class="button button--secondary button--compact" type="button" data-copy-value="${h(e.entryId)}" data-copy-label="Change ID copied">${icon('fa-copy')} Copy ID</button></div></div><div class="changelog-detail-grid"><article class="paper-card"><div class="paper-card__header"><div><h3>Impact</h3></div></div><div class="paper-card__body"><dl class="data-list"><div class="data-list__row"><dt>Change type</dt><dd>${h(e.changeType??e.category)}</dd></div><div class="data-list__row"><dt>Compatibility classification</dt><dd>${h(e.impact?.compatibilityClassification??'unknown')}</dd></div><div class="data-list__row"><dt>Breaking</dt><dd>${ynu(e.impact?.breakingChange)}</dd></div><div class="data-list__row"><dt>Migration required</dt><dd>${tri(e.impact?.migrationRequired)}</dd></div><div class="data-list__row"><dt>Security relevant</dt><dd>${e.impact?.securityRelevance?'Yes':'No'}</dd></div></dl></div></article><article class="paper-card"><div class="paper-card__header"><div><h3>Implementation action</h3><p>Unknown means the release evidence does not establish an action requirement.</p></div></div><div class="paper-card__body">${actionTable(e.impact)}</div></article></div><article class="paper-card"><div class="paper-card__header"><div><h3>Affected Registry IDs</h3><p>Explicitly recorded affected definitions only.</p></div></div><div class="paper-card__body">${affectedLinks(e.affectedRegistryIds??[])}</div></article><div class="changelog-detail-grid"><article class="paper-card"><div class="paper-card__header"><div><h3>Affected domains</h3></div></div><div class="paper-card__body"><div class="tag-cloud">${(e.affectedDomains??[]).map(x=>`<span class="tag">${h(x)}</span>`).join('')||'<span class="muted">None explicitly derivable.</span>'}</div></div></article><article class="paper-card"><div class="paper-card__header"><div><h3>Affected modules</h3></div></div><div class="paper-card__body"><div class="tag-cloud">${(e.affectedModules??[]).map(x=>`<a class="tag" href="#/registry/item/${encodeURIComponent(x)}">${h(x)}</a>`).join('')||'<span class="muted">None explicitly derivable.</span>'}</div></div></article></div><article class="paper-card"><div class="paper-card__header"><div><h3>Evidence and lifecycle links</h3></div></div><div class="paper-card__body"><dl class="data-list"><div class="data-list__row"><dt>Source / evidence</dt><dd><code>${h(e.sourceReference??'Unavailable')}</code></dd></div><div class="data-list__row"><dt>Source commit</dt><dd>${h(e.sourceCommitReference??'Unavailable')}</dd></div><div class="data-list__row"><dt>Per-change Diff evidence</dt><dd>${e.relatedDiffEvidence?h(JSON.stringify(e.relatedDiffEvidence)):'Unavailable'}</dd></div><div class="data-list__row"><dt>Migration guide</dt><dd>${h(e.relatedMigrationGuide??'Unavailable')}</dd></div><div class="data-list__row"><dt>Deprecation record</dt><dd>${h(e.relatedDeprecationRecord??'Unavailable')}</dd></div></dl><div class="changelog-actions-stack">${r?.evidence?.diffAvailable?`<a class="button button--secondary" href="${h(r.evidence.diffRoute)}">${icon('fa-code-compare')} Open release-level Contract Diff</a>`:''}<a class="button button--secondary" href="#/lifecycle/compatibility">${icon('fa-shield-halved')} Compatibility Center</a></div></div></article><article class="paper-card"><div class="paper-card__header"><div><h3>Raw machine-readable change JSON</h3><p>This exact entry is sourced from its authoritative release record.</p></div><button class="button button--ghost button--compact" type="button" data-copy-value="${h(JSON.stringify(e))}" data-copy-label="Change JSON copied">${icon('fa-copy')} Copy JSON</button></div><div class="paper-card__body"><pre class="code-block registry-raw"><code>${h(JSON.stringify(e,null,2))}</code></pre></div></article></div>`;}
function definitions(engine){return `<div class="stack stack--lg"><article class="changelog-callout"><span>${icon('fa-database')}</span><div><h2>Machine-readable lifecycle model</h2><p>Release, change-entry and impact schemas define the Changelog itself. They do not replace the contracts whose changes they describe.</p></div></article><div class="changelog-definition-grid">${engine.definitions.map(d=>`<article class="paper-card changelog-definition"><div><span class="tag">Schema</span><h3>${h(d.name)}</h3><code>${h(d.$id)}</code><p>${h(d.description)}</p></div><a class="button button--secondary button--compact" href="#/registry/item/${encodeURIComponent(d.$id)}">Inspect contract</a></article>`).join('')}</div></div>`;}
export function renderContractHistoryCard(engine,registryId){const row=engine.history(registryId);if(!row)return `<article class="paper-card" data-contract-change-history><div class="paper-card__header"><div><h2>Change history</h2><p>No Changelog history index entry is available for this Registry ID.</p></div></div></article>`;const latest=row.entries?.slice(0,8)??[];return `<article class="paper-card" data-contract-change-history><div class="paper-card__header"><div><h2>Change history</h2><p>Lifecycle navigation derived from authoritative <code>introducedIn</code> metadata and explicit Changelog affected IDs.</p></div><a class="button button--secondary button--compact" href="#/lifecycle/changelog?view=entries&affected=${encodeURIComponent(registryId)}">${icon('fa-clock-rotate-left')} Full history</a></div><div class="paper-card__body"><div class="changelog-history-grid"><div><span>Introduced in</span><strong>${row.introducedIn?`V${h(row.introducedIn)}`:'Unknown'}</strong></div><div><span>Latest related release</span><strong>${row.latestRelatedRelease?`V${h(row.latestRelatedRelease)}`:'Unknown'}</strong></div><div><span>Changed in</span><div>${versionLinks(row.changedIn)}</div></div><div><span>Deprecated in</span><div>${versionLinks(row.deprecatedIn)}</div></div><div><span>Removed in</span><div>${versionLinks(row.removedIn)}</div></div></div>${latest.length?`<div class="changelog-history-list"><h3>Latest related entries</h3>${latest.map(e=>`<a href="#/lifecycle/changelog?view=change&entry=${encodeURIComponent(e.entryId)}"><span>V${h(e.releaseVersion)} · ${h(e.category)}</span><strong>${h(e.title)}</strong><code>${h(e.entryId)}</code></a>`).join('')}</div>`:`<div class="registry-inline-empty">${icon('fa-circle-info')} No explicit affected change entry is recorded beyond introduction metadata.</div>`}</div></article>`;}
export function renderChangelogPage(engine,f){const view=f.view??'overview';let body;if(view==='releases'&&f.release)body=releaseDetail(engine,engine.release(f.release));else if(view==='releases')body=`<div class="stack">${filters(engine,f)}${releaseList(engine,f)}</div>`;else if(view==='change')body=changeDetail(engine,engine.entry(f.entry));else if(view==='entries')body=entries(engine,f);else if(view==='definitions')body=definitions(engine);else body=overview(engine);return `${pageHeader(engine)}${tabs(view)}${body}`;}
export function renderChangelogResults(engine,f){return f.view==='entries'?engine.searchEntries(f):engine.searchReleases(f);}
