import { escapeHtml } from "./utils.js";

const iconByKind = { registry:"fa-database", field:"fa-i-cursor", route:"fa-compass", document:"fa-book-open", release:"fa-code-branch", change:"fa-clock-rotate-left" };
const labelByKind = { registry:"Registry", field:"Field", route:"Portal", document:"Documentation", release:"Release", change:"Change" };

function h(value){ return escapeHtml(String(value ?? "")); }
function option(value, label, current){ return `<option value="${h(value)}" ${current===value?"selected":""}>${h(label)}</option>`; }
function resultHref(doc){ return h(doc.target); }
function targetAttrs(doc){ return doc.kind === "document" ? ' target="_blank" rel="noopener"' : ""; }

function mark(text, query){
  const source=String(text??"");
  const tokens=String(query??"").toLowerCase().replace(/[._/\\:-]+/g," ").match(/[a-z0-9]{2,}/g)??[];
  if(!tokens.length)return h(source);
  const escaped=tokens.slice(0,6).map(t=>t.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"));
  if(!escaped.length)return h(source);
  const re=new RegExp(`(${escaped.join("|")})`,`ig`);
  return h(source).replace(re,"<mark>$1</mark>");
}

export function renderGlobalSearchResults(engine, filters){
  const response=engine.search(filters.q,filters,{limit:100});
  if(!filters.q && !filters.kind && !filters.domain && !filters.status){
    return `<div class="search-page-empty"><span class="search-page-empty__icon"><i class="fa-solid fa-magnifying-glass" aria-hidden="true"></i></span><h2>Search the full NEXT F contract system</h2><p>Try <code>content.blogPost</code>, <code>order.paid</code>, <code>commerce.orders.manage</code>, “blog title”, “facebook tracking”, “ecommerce checkout” or any contract concept.</p><div class="search-examples"><button type="button" data-search-example="content.blogPost">content.blogPost</button><button type="button" data-search-example="order paid">order paid</button><button type="button" data-search-example="blog title">blog title</button><button type="button" data-search-example="facebook tracking">facebook tracking</button><button type="button" data-search-example="ecommerce checkout">ecommerce checkout</button></div></div>`;
  }
  if(!response.total){
    return `<div class="empty-state"><i class="fa-solid fa-magnifying-glass-minus" aria-hidden="true"></i><h3>No indexed result matches</h3><p>Try fewer words, a machine ID, a related term, or clear one of the filters. Search never fabricates missing contracts.</p></div>`;
  }
  return `<div class="global-search-result-list">${response.results.map(({doc,score,reasons})=>`<a class="global-search-result" href="${resultHref(doc)}"${targetAttrs(doc)}><span class="global-search-result__icon"><i class="fa-solid ${iconByKind[doc.kind]??"fa-file"}" aria-hidden="true"></i></span><span class="global-search-result__body"><span class="global-search-result__top"><strong>${mark(doc.title,filters.q)}</strong><span class="search-kind-chip">${h(labelByKind[doc.kind]??doc.kind)}</span></span><span class="global-search-result__subtitle">${mark(doc.subtitle||doc.machineId,filters.q)}</span><span class="global-search-result__description">${mark(doc.description,filters.q)}</span><span class="global-search-result__meta">${doc.machineId?`<code>${mark(doc.machineId,filters.q)}</code>`:""}${doc.domain?`<span>${h(doc.domain)}</span>`:""}${doc.status?`<span>${h(doc.status)}</span>`:""}${reasons?.[0]?`<span>${h(reasons[0])}</span>`:""}</span></span><span class="global-search-result__arrow"><i class="fa-solid fa-arrow-up-right-from-square" aria-hidden="true"></i><span class="sr-only">Open result</span></span></a>`).join("")}</div>`;
}

export function renderGlobalSearchPage(engine, filters){
  const response=engine.search(filters.q,filters,{limit:100});
  const sourceLabel=engine.source==="authoritative-json"?"Authoritative Search JSON":"Generated local fallback";
  return `<header class="page-header"><div><div class="eyebrow">Overview / Search</div><h1>Global Search</h1><p>One local discovery layer across canonical contracts, fields, Events, Webhooks, Permissions, Modules, APIs, examples, portal routes and documentation.</p></div><div class="header-actions"><span class="status-badge status-badge--stable">Phase 23</span><span class="tag">v0.24.0</span><a class="button button--secondary button--compact" href="./standards/31-global-search-standard.md"><i class="fa-solid fa-file-lines" aria-hidden="true"></i> Standard</a></div></header><div class="stack stack--lg"><div class="registry-source-bar" role="status"><span><i class="fa-solid fa-magnifying-glass" aria-hidden="true"></i> Search source</span><strong>${h(sourceLabel)}</strong></div><section class="dashboard-grid dashboard-grid--4"><article class="metric-card"><span>Search documents</span><strong>${engine.stats.documents}</strong><small>Local indexed records</small></article><article class="metric-card"><span>Registry items</span><strong>${engine.stats.registry}</strong><small>Canonical definitions</small></article><article class="metric-card"><span>Field records</span><strong>${engine.stats.fields}</strong><small>Contract-level field discovery</small></article><article class="metric-card"><span>Docs + routes</span><strong>${engine.stats.documentation+engine.stats.routes}</strong><small>Standards and navigation</small></article></section><section class="paper-card search-page-card"><div class="paper-card__header"><div><h2>Search everything</h2><p>Exact machine identifiers rank first. Natural language, controlled synonyms and bounded fuzzy matching improve discovery without changing canonical terminology.</p></div><button class="button button--ghost button--compact" type="button" data-global-search-clear><i class="fa-solid fa-rotate-left" aria-hidden="true"></i> Clear</button></div><div class="paper-card__body"><label class="global-search-input"><i class="fa-solid fa-magnifying-glass" aria-hidden="true"></i><span class="sr-only">Search NEXT F Contracts</span><input type="search" data-global-search-q autocomplete="off" value="${h(filters.q)}" placeholder="Search IDs, fields, Events, permissions, APIs, docs..."><kbd>⌘K</kbd></label><div class="global-search-filters"><label><span>Result type</span><select data-global-search-kind><option value="">All result types</option>${option("registry","Registry",filters.kind)}${option("field","Fields",filters.kind)}${option("route","Portal routes",filters.kind)}${option("document","Documentation",filters.kind)}${option("release","Releases",filters.kind)}${option("change","Change entries",filters.kind)}</select></label><label><span>Domain</span><select data-global-search-domain><option value="">All domains</option>${engine.domains.map(v=>option(v,v,filters.domain)).join("")}</select></label><label><span>Status</span><select data-global-search-status><option value="">All statuses</option>${engine.statuses.map(v=>option(v,v,filters.status)).join("")}</select></label><div class="field-filter-summary"><span>Results</span><strong data-global-search-count>${response.total}</strong></div></div></div></section><section class="search-page-results" data-global-search-results>${renderGlobalSearchResults(engine,filters)}</section><section class="paper-card"><div class="paper-card__header"><div><h2>Search behavior</h2><p>The search layer is deliberately local, deterministic and contract-first.</p></div></div><div class="paper-card__body"><div class="search-principles"><div><i class="fa-solid fa-bullseye" aria-hidden="true"></i><strong>Exact IDs first</strong><span>Canonical machine identifiers outrank fuzzy or descriptive matches.</span></div><div><i class="fa-solid fa-language" aria-hidden="true"></i><strong>Natural terminology</strong><span>Controlled synonyms connect terms such as ecommerce/store and Facebook/Meta.</span></div><div><i class="fa-solid fa-shield-halved" aria-hidden="true"></i><strong>Local privacy</strong><span>Queries are evaluated in the browser and do not require a remote search provider.</span></div><div><i class="fa-solid fa-link" aria-hidden="true"></i><strong>Canonical destination</strong><span>Every result returns to its registry item, portal route or source document.</span></div></div></div></section></div>`;
}
