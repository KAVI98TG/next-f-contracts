const h=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const icon=n=>`<i class="fa-solid ${n}" aria-hidden="true"></i>`;
const tags=a=>(a??[]).length?`<div class="a11y-tags">${a.map(x=>`<span class="tag">${h(x)}</span>`).join('')}</div>`:'<span class="muted">None</span>';
const regLinks=a=>(a??[]).length?`<div class="a11y-links">${a.map(x=>`<a href="#/registry/item/${encodeURIComponent(x)}"><code>${h(x)}</code></a>`).join('')}</div>`:'<span class="muted">None</span>';
const severity=s=>`<span class="a11y-severity a11y-severity--${h(s)}">${h(s)}</span>`;
function header(e){return `<header class="page-header"><div class="page-header__copy"><p class="page-header__eyebrow">Standards</p><h1>Accessibility</h1><p>Machine-readable accessibility requirements for customer websites, Customer CMS, NEXT F Admin and this Contract Portal.</p></div><div class="page-header__actions"><span class="status-badge status-badge--stable">Phase 31</span><span class="tag">${e.controls.length} controls</span></div></header><div class="a11y-source" role="status">${icon('fa-universal-access')} <span>Runtime source</span><strong>${e.source==='authoritative-json'?'Authoritative accessibility JSON':'Generated local fallback'}</strong><span class="tag">v${h(e.data.registryVersion)}</span></div>`;}
function tabs(v){const t=[['overview','Overview'],['controls','Controls'],['audit','Portal audit'],['surfaces','Surface coverage'],['definitions','Definitions']];return `<nav class="a11y-tabs" aria-label="Accessibility views">${t.map(([id,l])=>`<a class="${v===id?'is-active':''}" href="#/standards/accessibility?view=${id}">${h(l)}</a>`).join('')}</nav>`;}
function controlCard(c){return `<article class="paper-card a11y-control-card"><div class="a11y-control-card__top"><div><span class="a11y-control-id">${h(c.controlId)}</span><h3>${h(c.name)}</h3></div>${severity(c.severity)}</div><p>${h(c.requirement)}</p><div class="a11y-control-card__meta"><span class="tag">${h(c.category)}</span>${c.manualReviewRequired?'<span class="tag">manual review</span>':''}</div><a class="button button--secondary button--compact" href="#/standards/accessibility?view=control&id=${encodeURIComponent(c.controlId)}">Inspect control</a></article>`;}
function overview(e){const critical=e.controls.filter(x=>x.severity==='critical').length,manual=e.controls.filter(x=>x.manualReviewRequired).length,audit=e.portalAudit??{};return `<div class="stack stack--lg"><section class="dashboard-grid dashboard-grid--4"><article class="paper-card metric-card"><div class="metric-card__label">Controls</div><div class="metric-card__value">${e.controls.length}</div><p class="metric-card__meta">Canonical requirements</p></article><article class="paper-card metric-card"><div class="metric-card__label">Categories</div><div class="metric-card__value">${e.categories.length}</div><p class="metric-card__meta">Accessibility domains</p></article><article class="paper-card metric-card"><div class="metric-card__label">Critical</div><div class="metric-card__value">${critical}</div><p class="metric-card__meta">Critical blocking risks</p></article><article class="paper-card metric-card"><div class="metric-card__label">Manual review</div><div class="metric-card__value">${manual}</div><p class="metric-card__meta">Cannot be proven statically</p></article></section><div class="callout"><span class="callout__icon">${icon('fa-circle-info')}</span><div><h3>Automation does not prove accessibility</h3><p>Static checks are evidence, not certification. Controls explicitly declare keyboard, assistive-technology, visual, content or reflow review where human verification remains necessary.</p></div></div><section><div class="section-header"><div><h2>Category explorer</h2><p>Browse accessibility requirements by interaction domain.</p></div></div><div class="a11y-category-grid">${e.categories.map(c=>`<a class="paper-card a11y-category" href="#/standards/accessibility?view=controls&category=${encodeURIComponent(c.id)}"><span>${icon('fa-circle-check')}</span><div><h3>${h(c.label)}</h3><p>${h(c.description)}</p><strong>${e.controls.filter(x=>x.category===c.id).length} controls</strong></div></a>`).join('')}</div></section><section class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Portal baseline</h2></div></div><div class="paper-card__body"><p><strong>Status:</strong> ${h(audit.overallStatus??'not-run')}</p><p>${Number(audit.passed??0)} passed, ${Number(audit.failed??0)} failed, ${Number(audit.notRun??0)} not run.</p><a class="button button--secondary button--compact" href="#/standards/accessibility?view=audit">Review evidence</a></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Authoring integration</h2></div></div><div class="paper-card__body"><p>Accessibility metadata cross-links existing CMS editor definitions, Blocks and primitive Fields without duplicating their business-data contracts.</p><a class="button button--secondary button--compact" href="#/standards/accessibility?view=controls&q=author">Review authoring controls</a></div></article></section></div>`;}
function controls(e,f){const rows=e.search(f);const opts=(a,s,label=x=>x)=>a.map(x=>`<option value="${h(typeof x==='string'?x:x.id)}" ${(typeof x==='string'?x:x.id)===s?'selected':''}>${h(label(x))}</option>`).join('');return `<div class="stack stack--lg"><section class="paper-card a11y-controls"><div class="paper-card__header"><div><h2>Accessibility controls</h2><p><strong data-a11y-count>${rows.length}</strong> matching controls</p></div><button class="button button--ghost button--compact" type="button" data-a11y-clear>${icon('fa-rotate-left')} Clear</button></div><div class="paper-card__body a11y-filter-grid"><label class="field field--search"><span>Search</span><input type="search" value="${h(f.q)}" placeholder="Control ID, requirement, UI type..." data-a11y-q></label><label class="field"><span>Category</span><select data-a11y-category><option value="">All categories</option>${opts(e.categories,f.category,x=>x.label)}</select></label><label class="field"><span>Severity</span><select data-a11y-severity><option value="">All severities</option>${opts(e.severityLevels,f.severity,x=>x.label)}</select></label><label class="field"><span>UI type</span><select data-a11y-ui-type><option value="">All UI types</option>${opts(e.uiTypes,f.uiType,x=>x.label)}</select></label><label class="field"><span>Verification</span><select data-a11y-verification><option value="">All methods</option>${opts(e.verificationMethods,f.verification,x=>x.label)}</select></label><label class="field"><span>Applies to</span><select data-a11y-applicability><option value="">All surfaces</option>${opts(e.applicability(),f.applicability)}</select></label><label class="field"><span>Manual review</span><select data-a11y-manual><option value="">All</option><option value="yes" ${f.manual==='yes'?'selected':''}>Required</option><option value="no" ${f.manual==='no'?'selected':''}>Not required</option></select></label></div></section><div class="a11y-control-grid" data-a11y-results>${rows.map(controlCard).join('')||'<article class="paper-card"><div class="paper-card__body"><div class="empty-state"><h2>No controls match</h2><p>Adjust the filters.</p></div></div></article>'}</div></div>`;}
function detail(e,c){if(!c)return `<article class="paper-card"><div class="paper-card__body"><div class="empty-state"><h2>Accessibility control not found</h2></div></div></article>`;const refs=[...(c.relatedCmsEditorTypes??[]),...(c.relatedBlocks??[]),...(c.relatedFields??[]),...(c.relatedRegistryIds??[])];return `<div class="stack stack--lg"><a class="back-link" href="#/standards/accessibility?view=controls">${icon('fa-arrow-left')} All accessibility controls</a><section class="paper-card a11y-detail"><div class="paper-card__body"><div class="a11y-control-card__top"><div><span class="a11y-control-id">${h(c.controlId)}</span><h2>${h(c.name)}</h2></div>${severity(c.severity)}</div><p class="a11y-detail__requirement">${h(c.requirement)}</p><div class="a11y-control-card__meta"><span class="tag">${h(c.category)}</span>${c.manualReviewRequired?'<span class="tag">manual review required</span>':'<span class="tag">static/integration evidence may be sufficient</span>'}</div></div></section><div class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h3>Rationale & applicability</h3></div></div><div class="paper-card__body"><p>${h(c.rationale)}</p><h4>Applies to</h4>${tags(c.applicability)}<h4>UI types</h4>${tags(c.affectedUiTypes)}</div></article><article class="paper-card"><div class="paper-card__header"><div><h3>Verification</h3></div></div><div class="paper-card__body"><h4>Methods</h4>${tags(c.verificationMethods)}<p><strong>Manual review:</strong> ${c.manualReviewRequired?'required':'not required by this control'}</p></div></article></div><article class="paper-card"><div class="paper-card__header"><div><h3>Related canonical definitions</h3></div></div><div class="paper-card__body">${regLinks([...new Set(refs)])}</div></article><article class="paper-card"><div class="paper-card__header"><div><h3>Raw machine-readable JSON</h3></div><button class="button button--secondary button--compact" type="button" data-copy-value="${h(JSON.stringify(c,null,2))}" data-copy-label="Accessibility JSON copied">${icon('fa-copy')} Copy</button></div><div class="paper-card__body"><pre class="code-block"><code>${h(JSON.stringify(c,null,2))}</code></pre></div></article></div>`;}
function auditView(e){const a=e.portalAudit??{},checks=a.checks??[];const resultClass=r=>r==='pass'?'status-badge--stable':r==='fail'?'status-badge--danger':'status-badge--planned';return `<div class="stack stack--lg"><div class="callout"><span class="callout__icon">${icon('fa-clipboard-check')}</span><div><h3>Evidence-based portal audit</h3><p>Automated, source-review and browser-review evidence are separated. A PASS means the named Phase 31 baseline check passed; it is not a claim of universal accessibility certification.</p></div></div><section class="dashboard-grid dashboard-grid--4"><article class="paper-card metric-card"><div class="metric-card__label">Overall</div><div class="metric-card__value metric-card__value--text">${h(a.overallStatus??'not-run')}</div></article><article class="paper-card metric-card"><div class="metric-card__label">Passed</div><div class="metric-card__value">${Number(a.passed??0)}</div></article><article class="paper-card metric-card"><div class="metric-card__label">Failed</div><div class="metric-card__value">${Number(a.failed??0)}</div></article><article class="paper-card metric-card"><div class="metric-card__label">Not run</div><div class="metric-card__value">${Number(a.notRun??0)}</div></article></section><div class="a11y-audit-list">${checks.map(c=>`<article class="paper-card a11y-audit-card"><div class="paper-card__body"><div class="a11y-audit-card__top"><div><span class="a11y-control-id">${h(c.checkId)}</span><h3>${h(c.title)}</h3></div><span class="status-badge ${resultClass(c.result)}">${h(c.result)}</span></div><p>${h(c.evidence??'No evidence recorded.')}</p><div class="a11y-control-card__meta"><span class="tag">${h(c.reviewType)}</span><a href="#/standards/accessibility?view=control&id=${encodeURIComponent(c.controlId)}">${h(c.controlId)}</a></div></div></article>`).join('')}</div></div>`;}
function surfaces(e){return `<div class="a11y-surface-grid">${e.surfaceMapping.map(s=>`<article class="paper-card"><div class="paper-card__body"><div class="a11y-control-card__top"><h3>${h(s.label)}</h3><span class="status-badge status-badge--stable">${h(s.status)}</span></div><p><strong>${(s.controlIds??[]).length}</strong> applicable controls.</p><div class="a11y-links">${(s.controlIds??[]).slice(0,12).map(id=>`<a href="#/standards/accessibility?view=control&id=${encodeURIComponent(id)}"><code>${h(id)}</code></a>`).join('')}${(s.controlIds??[]).length>12?`<span class="tag">+${(s.controlIds??[]).length-12} more</span>`:''}</div></div></article>`).join('')}</div>`;}
function definitions(e){return `<div class="a11y-definition-grid">${e.definitions.map(x=>`<article class="paper-card"><div class="paper-card__body"><span class="a11y-control-id">${h(x.$id)}</span><h3>${h(x.name)}</h3><p>${h(x.description)}</p><a href="#/registry/item/${encodeURIComponent(x.$id)}">Registry definition</a></div></article>`).join('')}</div>`;}
export function renderAccessibilityResults(e,f){return e.search(f).map(controlCard).join('')||'<article class="paper-card"><div class="paper-card__body"><div class="empty-state"><h2>No accessibility controls match</h2></div></div></article>';}
export function renderAccessibilityPage(e,f={}){const view=f.view||'overview';let body=overview(e);if(view==='controls')body=controls(e,f);else if(view==='control')body=detail(e,e.get(f.id));else if(view==='audit')body=auditView(e);else if(view==='surfaces')body=surfaces(e);else if(view==='definitions')body=definitions(e);return `${header(e)}${tabs(view)}${body}`;}
