const e=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
const icon=n=>`<i class="fa-solid ${n}" aria-hidden="true"></i>`;
const reg=id=>`#/registry/item/${encodeURIComponent(id)}`;
const header=(t,d,a="")=>`<header class="page-header"><div><div class="eyebrow">Development / Codex</div><h1>${e(t)}</h1><p>${e(d)}</p></div>${a}</header>`;
const pill=(x)=>`<span class="tag">${e(x)}</span>`;
function rowId(view,x){if(view==="workflows")return `developer.workflow.${x.id}`;if(view==="tasks")return `developer.task.${x.id}`;if(view==="stops")return `developer.stop.${x.id}`;if(view==="artifacts")return `developer.artifact.${x.id}`;if(view==="changes")return `developer.change.${x.id}`;if(view==="gates")return `developer.gate.${x.id}`;return x.$id;}
function rowName(view,x){return x.label||x.name||x.$id;}
function rowDesc(view,x){return x.goal||x.reason||x.purpose||(x.checks?x.checks.join(" · "):"")||"Machine-readable Codex development definition.";}
function card(view,x){const id=rowId(view,x);const meta=[];if(x.order)meta.push(`Stage ${x.order}`);if(x.level)meta.push(x.level);if(x.compatibilityRisk)meta.push(`${x.compatibilityRisk} risk`);if(x.requiredStages)meta.push(`${x.requiredStages.length} stages`);if(x.requiredWhen)meta.push(`${x.requiredWhen.length} condition${x.requiredWhen.length===1?"":"s"}`);return `<a class="paper-card codex-card" href="${reg(id)}"><div class="codex-card__top"><span class="codex-icon">${icon(view==="workflows"?"fa-route":view==="tasks"?"fa-list-check":view==="stops"?"fa-octagon-exclamation":view==="artifacts"?"fa-file-circle-check":view==="changes"?"fa-code-compare":view==="gates"?"fa-shield-check":"fa-brackets-curly")}</span>${meta.length?`<span class="status-badge status-badge--stable">${e(meta[0])}</span>`:""}</div><h3>${e(rowName(view,x))}</h3><p>${e(rowDesc(view,x))}</p><div class="codex-card__meta"><code>${e(id)}</code>${meta.slice(1).map(pill).join("")}</div></a>`;}
export function renderCodexResults(engine,f){const rows=engine.search(f.q,f);if(!rows.length)return `<div class="empty-state"><i class="fa-solid fa-filter-circle-xmark" aria-hidden="true"></i><h3>No Codex definitions match</h3><p>Clear the search or choose another view.</p></div>`;return `<div class="codex-grid">${rows.map(x=>card(f.view,x)).join("")}</div>`;}
export function renderCodexIndex(engine,f){const c=engine.data.counts;const bootstrap=`NEXT F Codex preflight:\n1. Read AGENTS.md and standards/29-codex-development-standard.md.\n2. Read nextf.site.json and resolve the exact pinned Contract Version.\n3. Resolve selected Modules/Capabilities and canonical schemas, APIs, Permissions, Events, Webhooks, Integrations and CMS/Admin metadata.\n4. Do not invent parallel NEXT F architecture.\n5. Stop and report missing canonical coverage.\n6. Run relevant verification and report only checks actually executed.`;return `${header("Codex Development Standard","The permanent contract-first workflow for using Codex to build and maintain NEXT F customer websites and platform systems without architectural drift.",`<div class="header-actions"><span class="status-badge status-badge--stable">Phase 21</span><span class="tag">v${e(engine.data.registryVersion)}</span><a class="button button--secondary button--compact" href="./standards/29-codex-development-standard.md">${icon("fa-file-lines")} Standard</a></div>`)}<div class="stack stack--lg"><div class="registry-source-bar" role="status"><span>${icon("fa-database")} Runtime source</span><strong>${engine.source==="authoritative-json"?"Authoritative Developer Registry JSON":"Generated local fallback"}</strong></div><section class="dashboard-grid dashboard-grid--4"><article class="metric-card"><span>Workflow stages</span><strong>${c.workflowStages}</strong><small>Intake through handoff</small></article><article class="metric-card"><span>Task types</span><strong>${c.taskTypes}</strong><small>New Sites, bugs, APIs, Commerce and more</small></article><article class="metric-card"><span>Stop conditions</span><strong>${c.stopConditions}</strong><small>Explicit escalation boundaries</small></article><article class="metric-card"><span>Quality gates</span><strong>${c.qualityGates}</strong><small>Definition-of-done evidence</small></article></section><section class="paper-card"><div class="paper-card__header"><div><h2>Authority chain</h2><p>Codex implements the architecture; it does not redefine it.</p></div></div><div class="paper-card__body"><div class="codex-flow"><span>Repository + Task</span><b>→</b><span>Site Manifest</span><b>→</b><span>Pinned Contracts</span><b>→</b><span>Modules / APIs / Permissions</span><b>→</b><span>Implementation</span><b>→</b><span>Verification</span><b>→</b><span>Report</span></div></div></section><div class="dashboard-grid dashboard-grid--2"><section class="callout"><span class="callout__icon">${icon("fa-ban")}</span><div><h3>No parallel NEXT F architecture</h3><p>If a canonical schema, Event, Permission, API or Connector exists, reuse it. Missing shared coverage requires an Extension Proposal instead of a silent project-local replacement.</p></div></section><section class="callout"><span class="callout__icon">${icon("fa-circle-check")}</span><div><h3>Never fabricate verification</h3><p>A build, test, migration or deployment is reported as successful only when it was actually executed or directly verified. Unrun checks remain visible.</p></div></section></div><section class="paper-card"><div class="paper-card__header"><div><h2>Project bootstrap instruction</h2><p>Use this compact instruction when handing a NEXT F repository to Codex.</p></div><button class="button button--secondary button--compact" data-copy-value="${e(bootstrap)}" data-copy-label="Bootstrap instruction copied">${icon("fa-copy")} Copy</button></div><div class="paper-card__body"><pre class="code-block codex-bootstrap"><code>${e(bootstrap)}</code></pre><div class="inline-actions"><a class="button button--ghost button--compact" href="./developer/CODEX-QUICKSTART.md">${icon("fa-bolt")} Quick Start</a><a class="button button--ghost button--compact" href="./NEXT-F-WEBSITE-DEVELOPMENT-STANDARD.md">${icon("fa-book-open")} Master Entry Point</a></div></div></section><section class="paper-card"><div class="paper-card__header"><div><h2>Explore development rules</h2><p>Search the machine-readable workflow, task classes, stop conditions, artifacts, change classes, quality gates and report schemas.</p></div><button class="button button--ghost button--compact" data-codex-clear>${icon("fa-rotate-left")} Clear</button></div><div class="paper-card__body"><div class="registry-toolbar"><label class="search-field">${icon("fa-magnifying-glass")}<input data-codex-q type="search" value="${e(f.q)}" placeholder="Search manifest, commerce, extension, migration, security..."></label><select data-codex-view><option value="workflows" ${f.view==="workflows"?"selected":""}>Workflow stages</option><option value="tasks" ${f.view==="tasks"?"selected":""}>Task types</option><option value="stops" ${f.view==="stops"?"selected":""}>Stop conditions</option><option value="artifacts" ${f.view==="artifacts"?"selected":""}>Required artifacts</option><option value="changes" ${f.view==="changes"?"selected":""}>Change classes</option><option value="gates" ${f.view==="gates"?"selected":""}>Quality gates</option><option value="schemas" ${f.view==="schemas"?"selected":""}>Support schemas</option></select><div class="field-filter-summary"><span>Results</span><strong data-codex-count>${engine.search(f.q,f).length}</strong></div></div></div></section><section data-codex-results>${renderCodexResults(engine,f)}</section><section class="paper-card"><div class="paper-card__header"><div><h2>Extension path</h2><p>Missing reusable behavior is made explicit before it becomes permanent architecture.</p></div></div><div class="paper-card__body"><div class="codex-flow"><span>Requirement</span><b>→</b><span>Registry search</span><b>→</b><span>No canonical coverage</span><b>→</b><span>Extension Proposal</span><b>→</b><span>Approval / Site extension</span><b>→</b><span>Implementation</span></div></div></section></div>`;}
