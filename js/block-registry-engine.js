const loadGeneratedFallback = () => import("./generated-blocks.js");
function text(value){return String(value??"").trim().toLowerCase();}
function valid(data){return data&&typeof data.registryVersion==="string"&&Array.isArray(data.schemas)&&data.schemas.every((schema)=>schema&&typeof schema.$id==="string"&&/^blocks\./.test(schema.$id)&&typeof schema.name==="string"&&typeof schema.version==="string"&&schema.status==="stable"&&schema.domain==="blocks"&&typeof schema.category==="string"&&typeof schema.description==="string"&&typeof schema.purpose==="string"&&schema.blockModel&&Array.isArray(schema.fields)&&Array.isArray(schema.relationships)&&Array.isArray(schema.validationRules)&&schema.cms&&schema.presentation&&schema.accessibility&&schema.delivery&&schema.futureBindings&&schema.examples&&Array.isArray(schema.notes));}
async function loadJson(){if(window.location.protocol==="file:")throw new Error("file protocol uses generated Block fallback");const response=await fetch("./registry/blocks/index.json",{cache:"no-store"});if(!response.ok)throw new Error(`Block registry request failed with ${response.status}`);const data=await response.json();if(!valid(data))throw new Error("Block registry JSON failed runtime shape validation");return data;}
export class BlockRegistryEngine{
 constructor(data,source="generated",sourceDigest=null){if(!valid(data))throw new Error("Invalid Block registry data");this.data=data;this.source=source;this.sourceDigest=sourceDigest;this.schemas=Object.freeze(data.schemas.map((item)=>Object.freeze({...item})));this.byId=new Map(this.schemas.map((item)=>[item.$id,item]));this.categories=[...new Set(this.schemas.map((item)=>item.category))].sort();}
 get size(){return this.schemas.length;} get(id){return this.byId.get(id)??null;}
 search(query="",{category="",kind=""}={}){const q=text(query),c=text(category),k=text(kind);return this.schemas.filter((item)=>{if(c&&text(item.category)!==c)return false;if(k&&text(item.blockModel?.kind)!==k)return false;if(!q)return true;const fields=item.fields.flatMap((field)=>[field.key,field.primitive,field.schema,field.itemsSchema,field.description]);const hay=[item.$id,item.name,item.description,item.purpose,item.category,item.blockModel?.kind,...fields,...item.validationRules.flatMap((r)=>[r.id,r.description]),...(item.accessibility?.requirements??[])].join(" ").toLowerCase();return hay.includes(q);}).sort((a,b)=>a.name.localeCompare(b.name,undefined,{sensitivity:"base",numeric:true}));}
}
export async function loadBlocks(){try{return new BlockRegistryEngine(await loadJson(),"authoritative-json",null);}catch(error){console.info("Using generated Block registry fallback",error?.message??error);return new BlockRegistryEngine((await loadGeneratedFallback()).GENERATED_BLOCKS,"generated",(await loadGeneratedFallback()).GENERATED_BLOCKS_SOURCE_SHA256);}}
export function parseBlockFilters(search=""){const p=new URLSearchParams(search.replace(/^\?/,""));return{q:p.get("q")??"",category:p.get("category")??"",kind:p.get("kind")??""};}
export function buildBlockQuery({q="",category="",kind=""}={}){const p=new URLSearchParams();if(q)p.set("q",q);if(category)p.set("category",category);if(kind)p.set("kind",kind);const out=p.toString();return out?`?${out}`:"";}
