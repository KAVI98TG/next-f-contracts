import { escapeHtml } from "./utils.js";
const h=(v)=>escapeHtml(String(v??""));
const icon=(n)=>`<i class="fa-solid ${n}" aria-hidden="true"></i>`;
const option=(v,label,current)=>`<option value="${h(v)}" ${v===current?"selected":""}>${h(label)}</option>`;
const nodeHref=(id)=>`#/registry/item/${encodeURIComponent(id)}`;
const focusHref=(id)=>`#/registry/relationships?node=${encodeURIComponent(id)}`;
const familyIcon={composition:"fa-cubes-stacked",dependency:"fa-code-branch",reference:"fa-link",hierarchy:"fa-sitemap",governance:"fa-scale-balanced",derivation:"fa-wand-magic-sparkles",association:"fa-circle-nodes",authorization:"fa-key",event:"fa-bolt"};

function nodeChip(node){return `<a class="relationship-node-chip" href="${nodeHref(node.id)}"><strong>${h(node.name)}</strong><code>${h(node.id)}</code><span>${h(node.domain)} · ${h(node.type)}</span></a>`;}
function typeBadge(engine,type){const d=engine.type(type);return `<span class="relationship-type-badge" data-family="${h(d?.family??"association")}">${icon(familyIcon[d?.family]??"fa-link")} ${h(d?.label??type)}</span>`;}
function edgeOriginLabel(origin){return origin==="explicit"?"Explicit":origin==="permissionAssociation"?"Permission association":"Event association";}

function aggregateDirect(rows){const map=new Map();for(const row of rows){const key=`${row.orientation}|${row.neighbor.id}`;const v=map.get(key)??{orientation:row.orientation,neighbor:row.neighbor,edges:[]};v.edges.push(row.edge);map.set(key,v);}return [...map.values()];}
function relationCards(engine,rows,empty){const grouped=aggregateDirect(rows);if(!grouped.length)return `<div class="empty-state relationship-empty"><i class="fa-solid fa-link-slash" aria-hidden="true"></i><h3>${h(empty)}</h3><p>No indexed edge matches the current relationship filters.</p></div>`;return `<div class="relationship-neighbor-list">${grouped.slice(0,engine.config.defaults.maxRenderedNeighbors).map(g=>`<article class="relationship-neighbor"><div class="relationship-neighbor__head"><div>${nodeChip(g.neighbor)}</div><a class="icon-button" href="${focusHref(g.neighbor.id)}" aria-label="Focus ${h(g.neighbor.name)} in Relationship Explorer" title="Focus this node">${icon("fa-crosshairs")}</a></div><div class="relationship-neighbor__edges">${g.edges.map(e=>`<div class="relationship-edge-line">${typeBadge(engine,e.type)}<span class="relationship-origin">${h(edgeOriginLabel(e.origin))}</span>${g.edges.length>1?`<span class="tag">parallel</span>`:""}<p>${h(e.description||"Direct registry association.")}</p></div>`).join("")}</div></article>`).join("")}</div>${grouped.length>engine.config.defaults.maxRenderedNeighbors?`<p class="relationship-limit-note">Showing ${engine.config.defaults.maxRenderedNeighbors} of ${grouped.length} connected definitions. Narrow the filters to inspect the remainder.</p>`:""}`;}

function layers(engine,result){const populated=result.layers.filter(x=>x.nodes.length);if(!populated.length)return `<div class="empty-state relationship-empty"><i class="fa-solid fa-circle-nodes" aria-hidden="true"></i><h3>No expanded neighborhood</h3><p>No additional nodes are reachable within the selected depth and filters.</p></div>`;return `<div class="relationship-layers">${populated.map(layer=>`<section class="relationship-layer"><div class="relationship-layer__head"><strong>Hop ${layer.depth}</strong><span>${layer.nodes.length} new definition${layer.nodes.length===1?"":"s"}</span></div><div class="relationship-layer__nodes">${layer.nodes.slice(0,60).map(node=>`<div class="relationship-layer-node">${nodeChip(node)}<a href="${focusHref(node.id)}">Focus</a></div>`).join("")}</div></section>`).join("")}${result.truncated?`<div class="callout callout--warning"><i class="fa-solid fa-triangle-exclamation" aria-hidden="true"></i><div><strong>Traversal bounded</strong><p>The configured maximum expanded-node limit was reached. Narrow relationship filters or reduce depth.</p></div></div>`:""}</div>`;}

function focusPanel(engine,filters){const node=engine.resolveInput(filters.node);if(!node)return `<section class="paper-card"><div class="paper-card__body"><div class="search-page-empty"><span class="search-page-empty__icon">${icon("fa-circle-nodes")}</span><h2>Choose a Registry definition</h2><p>Enter an exact machine ID such as <code>commerce.order</code>, <code>content.blogPost</code>, <code>seo.metadata</code> or <code>order.paid</code>.</p><div class="search-examples"><button type="button" data-relationship-example="commerce.order">commerce.order</button><button type="button" data-relationship-example="content.blogPost">content.blogPost</button><button type="button" data-relationship-example="seo.metadata">seo.metadata</button><button type="button" data-relationship-example="order.paid">order.paid</button></div></div></div></section>`;
 const direct=engine.direct(node.id,filters),incoming=direct.filter(x=>x.orientation==="incoming"),outgoing=direct.filter(x=>x.orientation==="outgoing"),expanded=engine.neighborhood(node.id,filters);
 return `<section class="relationship-focus-map" aria-labelledby="relationship-focus-title"><article class="paper-card relationship-column"><div class="paper-card__header"><div><h2>Incoming</h2><p>Definitions pointing toward the focus.</p></div><span class="tag">${incoming.length}</span></div><div class="paper-card__body">${relationCards(engine,incoming,"No incoming relationship")}</div></article><article class="paper-card relationship-center"><div class="relationship-center__icon">${icon("fa-bullseye")}</div><span class="eyebrow">Focus definition</span><h2 id="relationship-focus-title">${h(node.name)}</h2><code>${h(node.id)}</code><p>${h(node.domain)} · ${h(node.type)} · ${h(node.status)}</p><div class="relationship-center__metrics"><span><strong>${node.incoming}</strong> incoming</span><span><strong>${node.outgoing}</strong> outgoing</span><span><strong>${node.degree}</strong> total</span></div><div class="relationship-center__actions"><a class="button button--primary button--compact" href="${nodeHref(node.id)}">${icon("fa-arrow-up-right-from-square")} Open contract</a><button class="button button--secondary button--compact" type="button" data-copy-value="${h(node.id)}" data-copy-label="Machine ID copied">${icon("fa-copy")} Copy ID</button></div></article><article class="paper-card relationship-column"><div class="paper-card__header"><div><h2>Outgoing</h2><p>Definitions reached from the focus.</p></div><span class="tag">${outgoing.length}</span></div><div class="paper-card__body">${relationCards(engine,outgoing,"No outgoing relationship")}</div></article></section>${Number(filters.depth)>1?`<section class="paper-card"><div class="paper-card__header"><div><h2>Expanded neighborhood</h2><p>Bounded traversal through ${h(filters.direction)} edges to ${h(filters.depth)} hops.</p></div><span class="tag">${expanded.nodes.length} nodes</span></div><div class="paper-card__body">${layers(engine,expanded)}</div></section>`:""}<section class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Impact review</h2><p>Incoming traversal helps identify definitions to inspect before changing this contract.</p></div></div><div class="paper-card__body relationship-impact-list">${impactRows(engine,node.id,"incoming")}</div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Dependency review</h2><p>Outgoing traversal helps inspect contracts the focused definition reaches.</p></div></div><div class="paper-card__body relationship-impact-list">${impactRows(engine,node.id,"outgoing")}</div></article></section>`;}

function impactRows(engine,id,direction){const r=engine.neighborhood(id,{direction,depth:3});return r.layers.map(l=>`<div class="relationship-impact-row"><span>Hop ${l.depth}</span><strong>${l.nodes.length}</strong><small>definition${l.nodes.length===1?"":"s"}</small></div>`).join("")||'<p class="muted">No reachable definitions.</p>';}

function pathResult(engine,filters){if(!filters.from||!filters.to)return `<div class="relationship-path-empty"><i class="fa-solid fa-route" aria-hidden="true"></i><p>Choose a source and target definition to trace the shortest indexed path.</p></div>`;const from=engine.resolveInput(filters.from),to=engine.resolveInput(filters.to);if(!from||!to)return `<div class="callout callout--warning"><i class="fa-solid fa-triangle-exclamation" aria-hidden="true"></i><div><strong>Unknown path endpoint</strong><p>Use an exact Registry machine ID for both endpoints.</p></div></div>`;const r=engine.path(from.id,to.id,{direction:filters.pathDirection,maxDepth:filters.pathDepth});if(!r.found)return `<div class="empty-state relationship-empty"><i class="fa-solid fa-route" aria-hidden="true"></i><h3>No indexed path found</h3><p>No path connects <code>${h(from.id)}</code> and <code>${h(to.id)}</code> within ${h(filters.pathDepth)} hops using ${filters.pathDirection==="outgoing"?"outgoing-only":"bidirectional"} traversal.</p></div>`;if(!r.steps.length)return `<div class="callout callout--info"><i class="fa-solid fa-circle-check" aria-hidden="true"></i><div><strong>Same definition</strong><p>The source and target are the same Registry node.</p></div></div>`;return `<div class="relationship-path" data-relationship-path>${r.steps.map((s,index)=>{const a=engine.get(s.from),b=engine.get(s.to);const actualForward=s.edge.source===s.from;return `<div class="relationship-path-step"><div class="relationship-path-node">${nodeChip(a)}</div><div class="relationship-path-edge"><span>Step ${index+1}</span>${typeBadge(engine,s.edge.type)}<small>${actualForward?"source → target":"traversed against edge direction"} · ${h(edgeOriginLabel(s.edge.origin))}</small><p>${h(s.edge.description||"")}</p></div>${index===r.steps.length-1?`<div class="relationship-path-node relationship-path-node--target">${nodeChip(b)}</div>`:""}</div>`;}).join("")}<div class="relationship-path-actions"><button class="button button--secondary button--compact" type="button" data-copy-relationship-path="${h(r.nodes.map(n=>n.id).join(' -> '))}">${icon("fa-copy")} Copy path</button><span>${r.steps.length} hop${r.steps.length===1?"":"s"}</span></div></div>`;}

function typeTable(engine){return `<div class="registry-table-wrap"><table class="registry-table"><thead><tr><th>Relationship</th><th>Family</th><th>Requiredness</th><th>Inverse reading</th><th>Origin</th></tr></thead><tbody>${engine.typeDefinitions.map(t=>`<tr><td><strong>${h(t.label)}</strong><code>${h(t.id)}</code><span>${h(t.description)}</span></td><td><span class="relationship-type-badge" data-family="${h(t.family)}">${icon(familyIcon[t.family]??"fa-link")} ${h(t.family)}</span></td><td>${h(t.requiredness)}</td><td>${h(t.inverseLabel)}</td><td>${t.graphOnly?'<span class="tag">Graph-derived</span>':'Registry relationships[]'}</td></tr>`).join("")}</tbody></table></div>`;}
function hubList(engine){return `<div class="relationship-hub-list">${engine.hubs(12).map((n,i)=>`<div class="relationship-hub-row"><span>${i+1}</span>${nodeChip(n)}<strong>${n.degree}</strong></div>`).join("")}</div>`;}
function domainFlows(engine){return `<div class="relationship-flow-list">${engine.domainFlows(16).map(x=>`<div class="relationship-flow-row"><code>${h(x.sourceDomain)}</code><i class="fa-solid fa-arrow-right" aria-hidden="true"></i><code>${h(x.targetDomain)}</code><strong>${x.count}</strong></div>`).join("")}</div>`;}

export function renderRelationshipExplorer(engine,filters){const sourceLabel=engine.source==="authoritative-json"?"Authoritative relationship JSON":"Generated local fallback";const current=engine.resolveInput(filters.node);return `<header class="page-header"><div><div class="eyebrow">Registry / Relationships</div><h1>Relationship Explorer</h1><p>Trace composition, dependencies, references, permissions, Events and cross-domain impact without inventing connections that do not exist in the Contract Registry.</p></div><div class="header-actions"><span class="status-badge status-badge--stable">Phase 24</span><span class="tag">v0.25.0</span><a class="button button--secondary button--compact" href="./standards/32-relationship-explorer-standard.md">${icon("fa-file-lines")} Standard</a></div></header><div class="stack stack--lg"><div class="registry-source-bar" role="status"><span>${icon("fa-circle-nodes")} Relationship source</span><strong>${h(sourceLabel)}</strong></div><section class="dashboard-grid dashboard-grid--4"><article class="metric-card"><span>Graph nodes</span><strong>${engine.stats.nodes}</strong><small>One per Registry item</small></article><article class="metric-card"><span>Graph edges</span><strong>${engine.stats.edges}</strong><small>${engine.stats.explicitEdges} explicit · ${engine.stats.permissionAssociations+engine.stats.eventAssociations} derived</small></article><article class="metric-card"><span>Relationship types</span><strong>${engine.stats.relationshipTypes}</strong><small>${engine.typeDefinitions.filter(x=>!x.graphOnly).length} source · ${engine.typeDefinitions.filter(x=>x.graphOnly).length} graph-only</small></article><article class="metric-card"><span>Isolated nodes</span><strong>${engine.stats.isolatedNodes}</strong><small>Review signal, not automatic error</small></article></section><section class="paper-card"><div class="paper-card__header"><div><h2>Focus explorer</h2><p>Inspect direct links or expand a bounded neighborhood around one canonical Registry definition.</p></div><button type="button" class="button button--ghost button--compact" data-relationship-clear>${icon("fa-rotate-left")} Clear</button></div><div class="paper-card__body"><div class="relationship-filter-grid"><label class="field relationship-node-field"><span>Focus machine ID</span><div class="field-control field-control--icon">${icon("fa-crosshairs")}<input type="search" list="relationship-node-options" value="${h(filters.node)}" placeholder="commerce.order" data-relationship-node autocomplete="off"></div></label><label class="field"><span>Direction</span><select data-relationship-direction>${option("both","Incoming + outgoing",filters.direction)}${option("outgoing","Outgoing only",filters.direction)}${option("incoming","Incoming only",filters.direction)}</select></label><label class="field"><span>Depth</span><select data-relationship-depth>${[1,2,3,4].map(n=>option(String(n),`${n} hop${n===1?"":"s"}`,String(filters.depth))).join("")}</select></label><label class="field"><span>Family</span><select data-relationship-family><option value="">All families</option>${engine.families.map(v=>option(v,v,filters.family)).join("")}</select></label><label class="field"><span>Type</span><select data-relationship-type><option value="">All relationship types</option>${engine.typeDefinitions.map(v=>option(v.id,v.label,filters.type)).join("")}</select></label><label class="field"><span>Origin</span><select data-relationship-origin><option value="">All origins</option>${option("explicit","Explicit relationships[]",filters.origin)}${option("permissionAssociation","Permission associations",filters.origin)}${option("eventAssociation","Event associations",filters.origin)}</select></label><label class="field"><span>Connected domain</span><select data-relationship-domain><option value="">All domains</option>${engine.domains.map(v=>option(v,v,filters.domain)).join("")}</select></label><div class="relationship-filter-summary"><span>Focus</span><strong data-relationship-focus-label>${current?h(current.id):"Not selected"}</strong></div></div><datalist id="relationship-node-options">${engine.nodes.map(n=>`<option value="${h(n.id)}">${h(n.name)}</option>`).join("")}</datalist></div></section><div data-relationship-focus-results>${focusPanel(engine,filters)}</div><section class="paper-card"><div class="paper-card__header"><div><h2>Path finder</h2><p>Find the shortest indexed route between two canonical definitions. Bidirectional discovery still shows each edge's real direction.</p></div><button class="button button--ghost button--compact" type="button" data-relationship-path-clear>${icon("fa-eraser")} Clear path</button></div><div class="paper-card__body"><div class="relationship-path-controls"><label class="field"><span>From</span><input type="search" list="relationship-node-options" value="${h(filters.from)}" placeholder="commerce.order" data-relationship-from></label><span class="relationship-path-arrow">${icon("fa-arrow-right")}</span><label class="field"><span>To</span><input type="search" list="relationship-node-options" value="${h(filters.to)}" placeholder="commerce.payment" data-relationship-to></label><label class="field"><span>Traversal</span><select data-relationship-path-direction>${option("both","Bidirectional discovery",filters.pathDirection)}${option("outgoing","Outgoing only",filters.pathDirection)}</select></label><label class="field"><span>Max hops</span><select data-relationship-path-depth>${[1,2,3,4,5,6,7,8].map(n=>option(String(n),String(n),String(filters.pathDepth))).join("")}</select></label></div><div data-relationship-path-results>${pathResult(engine,filters)}</div></div></section><section class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Most connected definitions</h2><p>Highest total indexed degree. High degree indicates broad registry connectivity, not runtime importance.</p></div></div><div class="paper-card__body">${hubList(engine)}</div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Top domain flows</h2><p>Largest source-domain to target-domain edge counts.</p></div></div><div class="paper-card__body">${domainFlows(engine)}</div></article></section><section class="paper-card"><div class="paper-card__header"><div><h2>Graph integrity</h2><p>Generated relationship projection health and review signals.</p></div></div><div class="paper-card__body"><div class="relationship-integrity-grid"><div><span>Explicit edges</span><strong>${engine.stats.explicitEdges}</strong></div><div><span>Permission associations</span><strong>${engine.stats.permissionAssociations}</strong></div><div><span>Event associations</span><strong>${engine.stats.eventAssociations}</strong></div><div><span>Parallel edge groups</span><strong>${engine.stats.parallelEdgeGroups}</strong></div><div><span>Self loops</span><strong>${engine.stats.selfLoops}</strong></div><div><span>Symbolic associations</span><strong>${engine.stats.symbolicAssociations}</strong></div><div><span>Unresolved explicit targets</span><strong>${engine.stats.unresolvedExplicitTargets}</strong></div><div><span>Source SHA-256</span><code title="${h(engine.index.sourceSha256)}">${h(engine.index.sourceSha256.slice(0,12))}…</code></div></div>${engine.stats.unresolvedExplicitTargets===0?`<div class="callout callout--success">${icon("fa-circle-check")}<div><strong>Explicit graph integrity passed</strong><p>Every explicit Registry relationship resolves to a canonical Registry node.</p></div></div>`:""}</div></section><section class="paper-card"><div class="paper-card__header"><div><h2>Relationship type reference</h2><p>Controlled interpretation metadata for the source relationship vocabulary and graph-only association edges.</p></div><span class="tag">${engine.typeDefinitions.length}</span></div><div class="paper-card__body">${typeTable(engine)}</div></section><section class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Isolated definitions</h2><p>Definitions with zero indexed graph degree. Isolation can be intentional.</p></div><span class="tag">${engine.stats.isolatedNodes}</span></div><div class="paper-card__body relationship-isolated-list">${engine.isolated(20).map(n=>nodeChip(n)).join("")||'<p class="muted">No isolated definitions.</p>'}${engine.stats.isolatedNodes>20?`<p class="relationship-limit-note">Showing 20 of ${engine.stats.isolatedNodes} isolated nodes.</p>`:""}</div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Interpretation guardrails</h2><p>Relationship discovery does not override contract authority.</p></div></div><div class="paper-card__body"><ul class="list-clean"><li class="list-item"><span class="list-item__icon">${icon("fa-route")}</span><div><strong>A graph path is not runtime call flow</strong><p>It only proves a sequence of indexed Registry associations.</p></div></li><li class="list-item"><span class="list-item__icon">${icon("fa-magnifying-glass")}</span><div><strong>Similarity creates no edges</strong><p>Matching names, tags or search terms never produce relationships.</p></div></li><li class="list-item"><span class="list-item__icon">${icon("fa-shield-halved")}</span><div><strong>Impact is a review signal</strong><p>Incoming reachability identifies definitions to inspect, not automatic breaking changes.</p></div></li><li class="list-item"><span class="list-item__icon">${icon("fa-database")}</span><div><strong>Registry remains authoritative</strong><p>The relationship index is a deterministic projection of canonical metadata.</p></div></li></ul></div></article></section></div>`;}

export function renderRelationshipFocusOnly(engine,filters){return focusPanel(engine,filters);}
export function renderRelationshipPathOnly(engine,filters){return pathResult(engine,filters);}
