import { icon, escapeHtml } from "./utils.js";
const e=escapeHtml;
const categoryLabels={foundation:"Foundation",authentication:"Authentication",configuration:"Configuration",mapping:"Mapping",runtime:"Runtime",sync:"Synchronization",health:"Health",resilience:"Resilience"};
function header(title,desc,actions=""){return `<header class="page-header"><div class="page-header__copy"><p class="page-header__eyebrow">Platform</p><h1>${e(title)}</h1><p>${e(desc)}</p></div>${actions?`<div class="page-header__actions">${actions}</div>`:""}</header>`;}
function sourceFor(item){const key=item.$id.split(".",2)[1].replace(/([a-z0-9])([A-Z])/g,"$1-$2").toLowerCase();return item.type==="connector"?`./registry/integrations/connectors/${key}.json`:`./registry/integrations/definitions/${key}.json`;}
function schemaSource(f){const id=f.primitive||f.schema||f.itemsPrimitive||f.itemsSchema;if(!id)return "—";return `<a href="#/registry/item/${encodeURIComponent(id)}"><code>${e(id)}</code></a>${f.itemsPrimitive||f.itemsSchema?' <span class="tag">many</span>':''}`;}
function connectorCards(engine,filters){const rows=engine.search(filters.q??"",{...filters,view:"connectors"});if(!rows.length)return `<div class="registry-empty paper-card"><span class="registry-empty__icon">${icon("fa-plug")}</span><h3>No connectors match</h3><p>Change the search or family filter.</p></div>`;return `<div class="connector-grid">${rows.map(c=>`<article class="paper-card integration-card"><div class="integration-card__top"><div><span class="connector-provider">${icon("fa-circle-nodes")} ${e(c.providerKey)}</span><h3><a href="#/registry/item/${encodeURIComponent(c.$id)}">${e(c.name)}</a></h3><code>${e(c.$id)}</code></div><span class="status-badge status-badge--stable">Stable</span></div><p>${e(c.description)}</p><div class="integration-card__meta"><span class="tag">${e(c.family)}</span>${c.runtimeModes.map(x=>`<span class="tag">${e(x)}</span>`).join("")}</div><div class="connector-capabilities">${c.capabilities.slice(0,6).map(x=>`<code>${e(x)}</code>`).join("")}${c.capabilities.length>6?`<span class="tag">+${c.capabilities.length-6}</span>`:""}</div><a class="integration-card__open" href="#/registry/item/${encodeURIComponent(c.$id)}">Inspect connector ${icon("fa-arrow-right")}</a></article>`).join("")}</div>`;}
function schemaCards(engine,filters){const rows=engine.search(filters.q??"",{...filters,view:"schemas"});if(!rows.length)return `<div class="registry-empty paper-card"><span class="registry-empty__icon">${icon("fa-diagram-project")}</span><h3>No integration schemas match</h3><p>Change the search or category filter.</p></div>`;return `<div class="integration-schema-grid">${rows.map(s=>`<article class="paper-card integration-card"><div class="integration-card__top"><div><span class="connector-provider">${e(categoryLabels[s.category]??s.category)}</span><h3><a href="#/registry/item/${encodeURIComponent(s.$id)}">${e(s.name)}</a></h3><code>${e(s.$id)}</code></div><span class="status-badge status-badge--stable">Stable</span></div><p>${e(s.description)}</p><div class="integration-card__meta"><span>${icon("fa-shapes")} ${e(s.integrationModel.kind)}</span><span>${icon("fa-list")} ${s.fields.length} fields</span><span>${icon("fa-circle-check")} ${s.validationRules.length} rules</span><span>${icon(s.delivery.publicAllowed?"fa-globe":"fa-lock")} ${s.delivery.publicAllowed?"Public eligible":"Private/operational"}</span></div><a class="integration-card__open" href="#/registry/item/${encodeURIComponent(s.$id)}">Inspect schema ${icon("fa-arrow-right")}</a></article>`).join("")}</div>`;}
export function renderIntegrationsResults(engine,filters={}){if(filters.view==="connectors")return connectorCards(engine,filters);if(filters.view==="schemas")return schemaCards(engine,filters);return `<div class="stack stack--lg"><section><div class="section-heading"><div><h2>Provider connectors</h2><p>Adapters for supported external platforms and generic integration families.</p></div></div>${connectorCards(engine,filters)}</section><section><div class="section-heading"><div><h2>Shared integration schemas</h2><p>Provider-neutral connection, auth, mapping, runtime, health and resilience contracts.</p></div></div>${schemaCards(engine,filters)}</section></div>`;}
export function renderIntegrationsIndex(engine,filters={}){const count=engine.search(filters.q??"",filters).length;const cats=engine.categories.map(x=>`<option value="${e(x)}" ${filters.category===x?"selected":""}>${e(categoryLabels[x]??x)}</option>`).join("");const fams=engine.families.map(x=>`<option value="${e(x)}" ${filters.family===x?"selected":""}>${e(x)}</option>`).join("");const capabilityList=engine.capabilities.map(x=>`<div class="capability-item"><code>${e(x.key)}</code><p>${e(x.label)} · ${e(x.runtime)}</p></div>`).join("");return `${header("Integrations","Provider connectors and reusable integration contracts that let NEXT F Sites add analytics, ads, search, CRM, email, webhooks and custom APIs after launch without redesigning the Site architecture.",`<div class="header-actions"><span class="status-badge status-badge--stable">Phase 10</span><span class="tag">${engine.schemas.length} schemas</span><span class="tag">${engine.connectors.length} connectors</span></div>`)}<div class="stack stack--lg">
<div class="registry-source-bar" role="status"><span>${icon("fa-database")} Runtime source</span><strong>${engine.source==="authoritative-json"?"Authoritative Integration JSON":"Generated local fallback"}</strong><span class="tag">v${e(engine.data.registryVersion)}</span></div>
<section class="integration-hero"><div class="paper-card"><div class="paper-card__body"><h2>One integration layer, many providers</h2><p>Sites emit canonical NEXT F events and use standard content/business contracts. Provider-specific IDs, authentication, resources and mappings live here instead of being hardcoded throughout customer Sites.</p><div class="integration-flow-row"><span class="integration-flow-node">Customer Site</span><span class="integration-flow-arrow">→</span><span class="integration-flow-node">NEXT F Runtime / API</span><span class="integration-flow-arrow">→</span><span class="integration-flow-node">Canonical Events & Data</span><span class="integration-flow-arrow">→</span><span class="integration-flow-node">Connector</span><span class="integration-flow-arrow">→</span><span class="integration-flow-node">External Provider</span></div></div></div><div class="paper-card"><div class="paper-card__body"><div class="integration-summary-grid"><div class="integration-summary"><span>Schemas</span><strong>${engine.schemas.length}</strong></div><div class="integration-summary"><span>Connectors</span><strong>${engine.connectors.length}</strong></div><div class="integration-summary"><span>Capabilities</span><strong>${engine.capabilities.length}</strong></div><div class="integration-summary"><span>Providers</span><strong>${engine.providers.length}</strong></div></div></div></div></section>
<div class="integration-boundary"><strong>Customer accounts stay customer-owned.</strong><p>NEXT F stores secure connection references and approved configuration. Raw secrets never enter public contract data or browser bundles, and server-side tracking does not bypass consent.</p></div>
<section class="paper-card"><div class="paper-card__header"><div><h2>Find integration definitions</h2><p>Search providers, configuration keys, capabilities, authentication, mappings, health and sync contracts.</p></div><button class="button button--ghost button--compact" type="button" data-integrations-clear>${icon("fa-rotate-left")} Clear</button></div><div class="paper-card__body"><div class="registry-toolbar"><label class="search-field"><span class="sr-only">Search integrations</span>${icon("fa-magnifying-glass")}<input type="search" value="${e(filters.q??"")}" placeholder="Search GTM, GA4, Meta, OAuth, CRM..." data-integrations-q></label><label><span class="sr-only">View</span><select data-integrations-view><option value="all" ${filters.view==="all"||!filters.view?"selected":""}>All</option><option value="connectors" ${filters.view==="connectors"?"selected":""}>Connectors</option><option value="schemas" ${filters.view==="schemas"?"selected":""}>Schemas</option></select></label><label><span class="sr-only">Category</span><select data-integrations-category><option value="">All schema categories</option>${cats}</select></label><label><span class="sr-only">Family</span><select data-integrations-family><option value="">All connector families</option>${fams}</select></label><div class="field-filter-summary"><span>Results</span><strong data-integrations-count>${count}</strong></div></div></div></section>
<section data-integrations-results>${renderIntegrationsResults(engine,filters)}</section>
<section class="paper-card"><div class="paper-card__header"><div><h2>Canonical capabilities</h2><p>Provider-independent operations connectors may declare. Sites enable capabilities rather than assuming behavior from a provider name.</p></div><a class="button button--secondary button--compact" href="./registry/integrations/capabilities.json">${icon("fa-file-code")} Capability JSON</a></div><div class="paper-card__body capability-list">${capabilityList}</div></section>
<section class="paper-card"><div class="paper-card__header"><div><h2>Phase 10 safety boundaries</h2></div><a class="button button--secondary button--compact" href="./standards/18-integration-contract-standard.md">${icon("fa-file-lines")} Standard</a></div><div class="paper-card__body"><div class="field-rule-grid"><div><strong>Secrets stay server-side</strong><p>OAuth tokens, API keys and signing secrets use opaque secret references.</p></div><div><strong>Consent applies everywhere</strong><p>Moving conversion/event dispatch to a server does not bypass Phase 9 consent and data rules.</p></div><div><strong>Environment isolation</strong><p>Preview, staging and production provider resources are explicitly bound.</p></div><div><strong>No arbitrary script runner</strong><p>Generic API and webhook connectors are allowlisted, typed and server-side by default.</p></div></div></div></section>
</div>`;}
export function renderIntegrationDetail(registry,item,definition){if(!definition)return null;if(definition.type==="connector")return renderConnectorDetail(item,definition);return renderSchemaDetail(item,definition);}
function renderConnectorDetail(item,c){const rows=c.configurationFields.map(f=>`<tr><td data-label="Field"><strong>${e(f.label)}</strong><code>${e(f.key)}</code><span>${e(f.helpText)}</span></td><td data-label="Primitive"><a href="#/registry/item/${encodeURIComponent(f.primitive)}"><code>${e(f.primitive)}</code></a></td><td data-label="Required">${f.required?'<span class="status-badge status-badge--stable">Required</span>':'Optional'}</td><td data-label="Client">${f.publicClientEligible?'<span class="tag">Eligible</span>':'Server/private'}</td><td data-label="Secret">${f.secret?'<span class="tag">Secret ref</span>':'No'}</td><td data-label="Customer">${f.customerEditable?'Editable':'NEXT F/System'}</td></tr>`).join("");return `${header(c.name,c.description,`<div class="header-actions"><span class="status-badge status-badge--stable">Stable</span><span class="tag">Connector</span><span class="tag">v${e(c.version)}</span></div>`)}<div class="stack stack--lg"><div class="registry-detail__identity"><div><span>Machine ID</span><code>${e(c.$id)}</code></div><div class="registry-detail__actions"><button class="button button--secondary button--compact" type="button" data-copy-value="${e(c.$id)}" data-copy-label="Connector ID copied">${icon("fa-copy")} Copy ID</button><a class="button button--secondary button--compact" href="${e(sourceFor(c))}">${icon("fa-file-code")} Source JSON</a><a class="button button--secondary button--compact" href="#/platform/integrations">${icon("fa-plug")} Integrations</a></div></div><section class="integration-detail-grid"><div class="integration-detail-cell"><span>Provider</span><strong>${e(c.providerKey)}</strong></div><div class="integration-detail-cell"><span>Family</span><strong>${e(c.family)}</strong></div><div class="integration-detail-cell"><span>Ownership</span><strong>${e(c.ownership)}</strong></div><div class="integration-detail-cell"><span>Authentication</span><strong>${e(c.authentication.type)}</strong></div></section><section class="paper-card"><div class="paper-card__header"><div><h2>Runtime & capabilities</h2><p>${c.runtimeModes.map(e).join(' · ')}</p></div></div><div class="paper-card__body"><div class="connector-capabilities">${c.capabilities.map(x=>`<code>${e(x)}</code>`).join("")}</div></div></section><section class="paper-card"><div class="paper-card__header"><div><h2>Configuration contract</h2><p>Only fields marked client eligible may be exposed to browser runtime.</p></div></div><div class="paper-card__body table-wrap"><table class="data-table config-table"><thead><tr><th>Field</th><th>Primitive</th><th>Required</th><th>Client</th><th>Secret</th><th>Editing</th></tr></thead><tbody>${rows}</tbody></table></div></section><div class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Consent</h2></div></div><div class="paper-card__body"><pre class="code-block"><code>${e(JSON.stringify(c.consent,null,2))}</code></pre></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Data handling</h2></div></div><div class="paper-card__body"><pre class="code-block"><code>${e(JSON.stringify(c.dataHandling,null,2))}</code></pre></div></article></div>${c.notes.length?`<article class="paper-card"><div class="paper-card__header"><div><h2>Connector notes</h2></div></div><div class="paper-card__body"><ul class="list-clean">${c.notes.map(n=>`<li class="list-item"><span class="list-item__icon">${icon("fa-circle-info")}</span><div>${e(n)}</div></li>`).join("")}</ul></div></article>`:""}<article class="paper-card"><div class="paper-card__header"><div><h2>Authoritative Connector JSON</h2></div><button class="button button--ghost button--compact" type="button" data-copy-value="${e(JSON.stringify(c))}" data-copy-label="Connector JSON copied">${icon("fa-copy")} Copy JSON</button></div><div class="paper-card__body"><pre class="code-block registry-raw"><code>${e(JSON.stringify(c,null,2))}</code></pre></div></article></div>`;}
function renderSchemaDetail(item,s){const rows=s.fields.map(f=>`<tr><td data-label="Field"><strong>${e(f.key)}</strong><span>${e(f.description)}</span></td><td data-label="Source">${schemaSource(f)}</td><td data-label="Required">${f.required?'<span class="status-badge status-badge--stable">Required</span>':'Optional'}</td><td data-label="Nullable">${f.nullable?'Yes':'No'}</td></tr>`).join("");const rels=s.relationships.length?s.relationships.map(r=>`<li class="list-item"><span class="list-item__icon">${icon("fa-link")}</span><div><strong>${e(r.type)}</strong> <a href="#/registry/item/${encodeURIComponent(r.target)}"><code>${e(r.target)}</code></a><p>${e(r.description)}</p></div></li>`).join(""):'<p class="muted">No explicit relationships.</p>';const rules=s.validationRules.map(r=>`<div class="marketing-rule"><strong><code>${e(r.id)}</code></strong><p>${e(r.description)}</p></div>`).join("");return `${header(s.name,s.description,`<div class="header-actions"><span class="status-badge status-badge--stable">Stable</span><span class="tag">${e(categoryLabels[s.category]??s.category)}</span><span class="tag">v${e(s.version)}</span></div>`)}<div class="stack stack--lg"><div class="registry-detail__identity"><div><span>Machine ID</span><code>${e(s.$id)}</code></div><div class="registry-detail__actions"><button class="button button--secondary button--compact" type="button" data-copy-value="${e(s.$id)}" data-copy-label="Integration ID copied">${icon("fa-copy")} Copy ID</button><a class="button button--secondary button--compact" href="${e(sourceFor(s))}">${icon("fa-file-code")} Source JSON</a><a class="button button--secondary button--compact" href="#/platform/integrations">${icon("fa-plug")} Integrations</a></div></div><section class="integration-detail-grid"><div class="integration-detail-cell"><span>Category</span><strong>${e(categoryLabels[s.category]??s.category)}</strong></div><div class="integration-detail-cell"><span>Model</span><strong>${e(s.integrationModel.kind)}</strong></div><div class="integration-detail-cell"><span>Data</span><strong>${e(s.integrationModel.dataSensitivity)}</strong></div><div class="integration-detail-cell"><span>Customer managed</span><strong>${s.integrationModel.customerManaged?'Eligible':'No'}</strong></div></section><section class="paper-card"><div class="paper-card__header"><div><h2>Fields</h2><p>${e(s.purpose)}</p></div></div><div class="paper-card__body table-wrap"><table class="data-table"><thead><tr><th>Field</th><th>Source</th><th>Required</th><th>Nullable</th></tr></thead><tbody>${rows}</tbody></table></div></section><div class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Relationships</h2></div></div><div class="paper-card__body"><ul class="list-clean">${rels}</ul></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Validation rules</h2></div></div><div class="paper-card__body stack">${rules}</div></article></div>${s.notes.length?`<article class="paper-card"><div class="paper-card__header"><div><h2>Implementation notes</h2></div></div><div class="paper-card__body"><ul class="list-clean">${s.notes.map(n=>`<li class="list-item"><span class="list-item__icon">${icon("fa-circle-info")}</span><div>${e(n)}</div></li>`).join("")}</ul></div></article>`:""}<article class="paper-card"><div class="paper-card__header"><div><h2>Authoritative Integration JSON</h2></div><button class="button button--ghost button--compact" type="button" data-copy-value="${e(JSON.stringify(s))}" data-copy-label="Integration JSON copied">${icon("fa-copy")} Copy JSON</button></div><div class="paper-card__body"><pre class="code-block registry-raw"><code>${e(JSON.stringify(s,null,2))}</code></pre></div></article></div>`;}
