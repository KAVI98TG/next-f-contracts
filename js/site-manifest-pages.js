import { icon, escapeHtml } from "./utils.js";
const e=escapeHtml;
function header(title,desc,actions=""){return `<header class="page-header"><div class="page-header__copy"><p class="page-header__eyebrow">Development</p><h1>${e(title)}</h1><p>${e(desc)}</p></div>${actions?`<div class="page-header__actions">${actions}</div>`:""}</header>`;}
function sourceForSchema(s){const name=s.$id.split(".",2)[1].replace(/([a-z0-9])([A-Z])/g,"$1-$2").toLowerCase();return `./registry/manifests/definitions/${name}.json`;}
function fieldSource(f){if(f.schema)return `<a href="#/registry/item/${encodeURIComponent(f.schema)}"><code>${e(f.schema)}</code></a>`;if(f.primitive)return `<a href="#/registry/item/${encodeURIComponent(f.primitive)}"><code>${e(f.primitive)}</code></a>`;return "—";}
function cards(engine,filters={}){const rows=engine.search(filters.q??"",filters);if(!rows.length)return `<div class="registry-empty paper-card"><span class="registry-empty__icon">${icon("fa-file-code")}</span><h3>No manifest schemas match</h3><p>Change the search or category filters.</p></div>`;return `<div class="manifest-schema-grid">${rows.map(s=>`<article class="paper-card manifest-schema-card"><div class="manifest-schema-card__top"><div><h3><a href="#/registry/item/${encodeURIComponent(s.$id)}">${e(s.name)}</a></h3><code>${e(s.$id)}</code></div><span class="status-badge status-badge--stable">${e(s.manifestModel?.kind??"schema")}</span></div><p>${e(s.description)}</p><div class="manifest-schema-card__meta"><span class="tag">${e(s.category)}</span><span class="tag">${s.fields.length} fields</span><span class="tag">${s.validationRules.length} rules</span></div></article>`).join("")}</div>`;}
export function renderSiteManifestSchemasOnly(engine,filters={}){return cards(engine,filters);}
export function renderSiteManifestExample(engine,id){const ex=engine.example(id);if(!ex)return `<div class="registry-empty">No example available.</div>`;const src=`./registry/manifests/examples/${e(ex.id)}.json`;return `<div class="manifest-example-panel"><div class="manifest-example-panel__header"><div><strong>${e(ex.name)}</strong><span>${e(ex.siteType)}</span></div><div class="header-actions"><button class="button button--secondary button--compact" type="button" data-copy-value="${e(JSON.stringify(ex.data??{}))}" hidden></button><a class="button button--secondary button--compact" href="${src}">${icon("fa-file-code")} Source</a><button class="button button--secondary button--compact" type="button" data-copy-manifest-example="${e(ex.id)}">${icon("fa-copy")} Copy JSON</button></div></div><pre class="code-block registry-raw manifest-example-code"><code>${e(JSON.stringify(ex.data??{},null,2))}</code></pre></div>`;}
export function renderSiteManifestIndex(engine,filters={}){
  const example=engine.example(filters.example)||engine.example("commerce")||engine.examples[0];
  const catOpts=engine.categories.map(x=>`<option value="${e(x.id)}" ${filters.category===x.id?"selected":""}>${e(x.label)}</option>`).join("");
  const models=[...new Set(engine.schemas.map(x=>x.manifestModel?.kind).filter(Boolean))].sort();
  const modelOpts=models.map(x=>`<option value="${e(x)}" ${filters.model===x?"selected":""}>${e(x)}</option>`).join("");
  const exOpts=engine.examples.map(x=>`<option value="${e(x.id)}" ${example?.id===x.id?"selected":""}>${e(x.name)} · ${e(x.siteType)}</option>`).join("");
  return `${header("Site Manifest","One authoritative nextf.site.json tells Codex and NEXT F tooling what a customer Site is, which contract release it uses, what it implements and which integrations it can support.",`<div class="header-actions"><span class="status-badge status-badge--stable">Phase 16</span><span class="tag">Spec v${e(engine.data.schemaVersion)}</span><a class="button button--secondary button--compact" href="./registry/manifests/nextf-site-manifest.schema.json">${icon("fa-code")} JSON Schema</a></div>`)}
  <div class="stack stack--lg manifest-page">
    <div class="registry-source-bar" role="status"><span>${icon("fa-database")} Runtime source</span><strong>${engine.source==="authoritative-json"?"Authoritative Site Manifest JSON":"Generated local fallback"}</strong><span class="tag">Contracts v${e(engine.data.registryVersion)}</span></div>
    <section class="manifest-hero-grid"><article class="paper-card"><div class="paper-card__body"><div class="manifest-file-label">${icon("fa-file-code")} <code>nextf.site.json</code></div><h2>Read this before building the Site.</h2><p>The Manifest is the repository-side handshake between customer-specific code and the reusable NEXT F contract ecosystem. It pins versions, declares functionality and prevents Codex from inventing a different CMS/API/integration architecture for every project.</p><div class="manifest-flow"><span>Customer project</span><b>→</b><span>Site Manifest</span><b>→</b><span>Pinned Contracts</span><b>→</b><span>Modules + Runtime</span><b>→</b><span>Build</span></div></div></article><article class="paper-card"><div class="paper-card__body manifest-summary-grid"><div><span>Manifest schemas</span><strong>${engine.schemas.length}</strong></div><div><span>Site types</span><strong>${engine.siteTypes.length}</strong></div><div><span>Runtime primitives</span><strong>${engine.runtimeCapabilities.length}</strong></div><div><span>Examples</span><strong>${engine.examples.length}</strong></div></div></article></section>
    <div class="integration-boundary"><strong>Manifest is configuration metadata, not a secret store.</strong><p>URLs and required configuration key names may be declared. API secrets, OAuth tokens, webhook signing secrets, database credentials, hosting credentials and customer content are prohibited.</p></div>
    <section class="paper-card"><div class="paper-card__header"><div><h2>Manifest anatomy</h2><p>Every important implementation boundary has a dedicated declaration instead of an unstructured settings object.</p></div><a class="button button--secondary button--compact" href="./standards/24-site-manifest-standard.md">${icon("fa-file-lines")} Standard</a></div><div class="paper-card__body manifest-anatomy-grid">
      ${[["contracts","Exact Contract + Manifest spec versions","fa-code-branch"],["site","Canonical Site ID + descriptive type","fa-globe"],["localization","Locale + IANA timezone","fa-language"],["environments","Development / preview / staging / production","fa-layer-group"],["contentDelivery","Runtime / build-time / hybrid","fa-arrows-rotate"],["modules","Functional modules + capabilities","fa-table-cells-large"],["runtime","Five mandatory NEXT F runtime primitives","fa-microchip"],["cms","Structured Customer CMS boundary","fa-pen-to-square"],["apiBindings","Future Phase 18 API version bindings","fa-code"],["events","Canonical Phase 13 business-event support","fa-bolt"],["tracking","Phase 9 measurement-event support","fa-chart-line"],["integrations","Phase 10 connector support","fa-plug"],["configuration","Key metadata + exposure class, never values","fa-key"],["extensions","Explicit namespaced project extensions","fa-puzzle-piece"]].map(([k,d,i])=>`<div class="manifest-anatomy-item"><span>${icon(i)}</span><div><code>${e(k)}</code><p>${e(d)}</p></div></div>`).join("")}
    </div></section>
    <section class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Mandatory runtime baseline</h2><p>Every fully integrated NEXT F Site starts ready for future management and integrations.</p></div></div><div class="paper-card__body"><ul class="list-clean">${engine.runtimeCapabilities.map(x=>`<li class="list-item"><span class="list-item__icon">${icon("fa-circle-check")}</span><div><strong>${e(x.label)}</strong><p>${e(x.description)}</p></div></li>`).join("")}</ul></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Hard boundaries</h2><p>Things that never belong in nextf.site.json.</p></div></div><div class="paper-card__body"><div class="manifest-boundary-list">${["API/OAuth/Webhook secrets","Database or payment credentials","Hosting-control credentials","Domain registrar credentials","Lead/submission/customer records","Blog/page/product body content","Arbitrary executable code","A floating contract version such as latest"].map(x=>`<span>${icon("fa-circle-xmark")} ${e(x)}</span>`).join("")}</div></div></article></section>
    <section class="paper-card"><div class="paper-card__header"><div><h2>Reference manifests</h2><p>Examples demonstrate structure only. They do not replace customer requirements or future Module Registry rules.</p></div><label class="manifest-example-select"><span class="sr-only">Example Site</span><select data-manifest-example>${exOpts}</select></label></div><div class="paper-card__body" data-manifest-example-panel>${renderSiteManifestExample(engine,example?.id)}</div></section>
    <section class="paper-card"><div class="paper-card__header"><div><h2>Manifest contracts</h2><p>Inspect every reusable declaration used by the Site Manifest.</p></div><button class="button button--ghost button--compact" data-manifest-clear>${icon("fa-rotate-left")} Clear</button></div><div class="paper-card__body"><div class="registry-toolbar"><label class="search-field">${icon("fa-magnifying-glass")}<input type="search" value="${e(filters.q??"")}" placeholder="Search environment, runtime, integration..." data-manifest-q></label><label><span class="sr-only">Category</span><select data-manifest-category><option value="">All categories</option>${catOpts}</select></label><label><span class="sr-only">Model</span><select data-manifest-model><option value="">All models</option>${modelOpts}</select></label><div class="field-filter-summary"><span>Results</span><strong data-manifest-count>${engine.search(filters.q??"",filters).length}</strong></div></div></div></section>
    <section data-manifest-results>${cards(engine,filters)}</section>
    <section class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Phase boundaries</h2></div></div><div class="paper-card__body"><div class="field-rule-grid"><div><strong>Phase 17</strong><p>Publishes the canonical Module/Capability Registry and dependency graph.</p></div><div><strong>Phase 18</strong><p>Publishes canonical API IDs, surfaces and compatibility contracts.</p></div><div><strong>Phase 21</strong><p>Builds the dedicated Codex development workflow on top of this Manifest.</p></div><div><strong>Phase 33</strong><p>Adds interactive/CLI validation rather than duplicating validation logic inside this page.</p></div></div></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Ownership boundary</h2></div></div><div class="paper-card__body"><p>The Manifest can contain production/preview URLs because code needs technical references, but this never means NEXT F provides domain registration or hosting services. Customer infrastructure remains customer-owned and provider-independent.</p><p class="manifest-note">Site type is descriptive. Modules and capabilities define functionality.</p></div></article></section>
  </div>`;
}
export function renderSiteManifestSchemaDetail(schema){
  const rows=schema.fields.map(f=>`<tr><td><code>${e(f.key)}</code></td><td>${fieldSource(f)}</td><td>${f.required?"Yes":"No"}</td><td>${f.nullable?"Yes":"No"}</td><td>${e(f.description)}</td></tr>`).join("");
  const rel=(schema.relationships??[]).map(r=>`<a class="manifest-relation" href="#/registry/item/${encodeURIComponent(r.target)}"><span>${e(r.type)}</span><code>${e(r.target)}</code><small>${e(r.description)}</small></a>`).join("")||`<p class="empty-note">No direct schema relationships.</p>`;
  const notes=(schema.notes??[]).map(n=>`<li class="list-item"><span class="list-item__icon">${icon("fa-circle-info")}</span><div>${e(n)}</div></li>`).join("")||`<li class="list-item"><span class="list-item__icon">—</span><div>No additional notes.</div></li>`;
  return `${header(schema.name,schema.description,`<div class="header-actions"><span class="status-badge status-badge--stable">${e(schema.manifestModel?.kind??"schema")}</span><button class="button button--secondary button--compact" data-copy-value="${e(schema.$id)}" data-copy-label="Manifest contract ID copied">${icon("fa-copy")} Copy ID</button><a class="button button--secondary button--compact" href="${sourceForSchema(schema)}">${icon("fa-file-code")} Source</a></div>`)}<div class="stack stack--lg"><section class="manifest-detail-grid"><div><span>Contract ID</span><code>${e(schema.$id)}</code></div><div><span>Category</span><strong>${e(schema.category)}</strong></div><div><span>Version</span><strong>v${e(schema.version)}</strong></div><div><span>Repository managed</span><strong>${schema.manifestModel?.repositoryManaged?"Yes":"No"}</strong></div><div><span>Customer managed</span><strong>${schema.manifestModel?.customerManaged?"Yes":"No"}</strong></div><div><span>Public definition</span><strong>${schema.delivery?.publicAllowed?"Yes":"No"}</strong></div></section><article class="paper-card"><div class="paper-card__header"><div><h2>Purpose</h2></div></div><div class="paper-card__body"><p>${e(schema.purpose)}</p></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Fields</h2><p>Primitive and nested contracts remain authoritative.</p></div></div><div class="paper-card__body table-wrap"><table class="data-table"><thead><tr><th>Field</th><th>Source</th><th>Required</th><th>Nullable</th><th>Description</th></tr></thead><tbody>${rows}</tbody></table></div></article><div class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Validation rules</h2></div></div><div class="paper-card__body stack">${schema.validationRules.map(r=>`<div class="rule-row"><div class="rule-row__id">${e(r.id)}</div><div>${e(r.description)}</div></div>`).join("")}</div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Relationships</h2></div></div><div class="paper-card__body manifest-relations">${rel}</div></article></div><article class="paper-card"><div class="paper-card__header"><div><h2>Implementation notes</h2></div></div><div class="paper-card__body"><ul class="list-clean">${notes}</ul></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Authoritative contract JSON</h2></div><button class="button button--ghost button--compact" data-copy-value="${e(JSON.stringify(schema))}" data-copy-label="Manifest contract JSON copied">${icon("fa-copy")} Copy JSON</button></div><div class="paper-card__body"><pre class="code-block registry-raw"><code>${e(JSON.stringify(schema,null,2))}</code></pre></div></article></div>`;
}
