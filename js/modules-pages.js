import { icon, escapeHtml } from "./utils.js";
const e=escapeHtml;
const h=(t,d,a="")=>`<header class="page-header"><div class="page-header__copy"><p class="page-header__eyebrow">Platform</p><h1>${e(t)}</h1><p>${e(d)}</p></div>${a?`<div class="page-header__actions">${a}</div>`:""}</header>`;
const link=id=>`#/registry/item/${encodeURIComponent(id)}`;
const capabilityLink=id=>link(`modules.capability.${id}`);
function moduleCard(m,engine){const caps=engine.capabilitiesFor(m.moduleId);return `<article class="paper-card module-card"><div class="module-card__top"><span class="module-icon">${icon(m.icon||"fa-table-cells-large")}</span><div><h3><a href="${link(m.$id)}">${e(m.name)}</a></h3><code>${e(m.moduleId)}</code></div><span class="status-badge status-badge--stable">${m.selection.mandatory?"Required":"Module"}</span></div><p>${e(m.description)}</p><div class="module-card__meta"><span class="tag">${e(m.category)}</span><span class="tag">${caps.length} capabilities</span><span class="tag">${m.dependencies.filter(x=>x.kind==="required").length} required deps</span><span class="tag">${m.contractBindings.length} contracts</span></div></article>`;}
function capabilityCard(c){return `<article class="paper-card module-card"><div class="module-card__top"><span class="module-icon module-icon--small">${icon("fa-toggle-on")}</span><div><h3><a href="${link(c.$id)}">${e(c.name)}</a></h3><code>${e(c.capabilityId)}</code></div><span class="status-badge status-badge--stable">${e(c.mode)}</span></div><p>${e(c.description)}</p><div class="module-card__meta"><span class="tag">${e(c.moduleId)}</span><span class="tag">${c.contractBindings.length} contracts</span><span class="tag">${c.defaultEnabled?"Default enabled":"Explicit opt-in"}</span></div></article>`;}
export function renderModuleResults(engine,f={}){const rows=engine.search(f.q??"",f);if(!rows.length)return `<div class="registry-empty paper-card"><span class="registry-empty__icon">${icon("fa-table-cells-large")}</span><h3>No module registry results</h3><p>Change the search or filters.</p></div>`;return `<div class="module-grid">${rows.map(x=>f.view==="capabilities"?capabilityCard(x):moduleCard(x,engine)).join("")}</div>`;}
export function renderModulesIndex(engine,f={}){const cats=engine.categories.map(x=>`<option value="${e(x.id)}" ${f.category===x.id?"selected":""}>${e(x.label)}</option>`).join("");const deps=(engine.data.dependencyKinds??[]).map(x=>`<option value="${e(x.id)}" ${f.dependency===x.id?"selected":""}>${e(x.label)}</option>`).join("");const modes=(engine.data.capabilityModes??[]).map(x=>`<option value="${e(x.id)}" ${f.mode===x.id?"selected":""}>${e(x.label)}</option>`).join("");return `${h("Module Registry","One authoritative module/capability graph tells Site Manifests, Codex, Customer CMS and NEXT F Admin exactly what functionality a customer website supports.",`<div class="header-actions"><span class="status-badge status-badge--stable">Phase 17</span><span class="tag">v${e(engine.data.registryVersion)}</span><a class="button button--secondary button--compact" href="./standards/25-module-registry-standard.md">${icon("fa-file-lines")} Standard</a></div>`)}<div class="stack stack--lg"><div class="registry-source-bar" role="status"><span>${icon("fa-database")} Runtime source</span><strong>${engine.source==="authoritative-json"?"Authoritative Module JSON":"Generated local fallback"}</strong></div><section class="dashboard-grid dashboard-grid--4"><article class="metric-card"><span>Modules</span><strong>${engine.modules.length}</strong><small>Canonical Site modules</small></article><article class="metric-card"><span>Capabilities</span><strong>${engine.capabilities.length}</strong><small>Module-scoped features</small></article><article class="metric-card"><span>Required dependency edges</span><strong>${engine.modules.flatMap(x=>x.dependencies).filter(x=>x.kind==="required").length}</strong><small>Strict Manifest requirements</small></article><article class="metric-card"><span>Reserved capabilities</span><strong>${engine.reservedCapabilities.length}</strong><small>Not selectable yet</small></article></section><section class="paper-card"><div class="paper-card__header"><div><h2>Manifest resolution order</h2><p>Site functionality is explicit, dependency-aware and contract-first.</p></div></div><div class="paper-card__body"><div class="manifest-flow"><span>nextf.site.json</span><b>→</b><span>Module IDs</span><b>→</b><span>Required Dependencies</span><b>→</b><span>Capabilities</span><b>→</b><span>Contracts + Permissions + Events</span><b>→</b><span>Build</span></div></div></section><section class="paper-card"><div class="paper-card__header"><div><h2>Explore modules and capabilities</h2><p>Unknown IDs are invalid. Capabilities belong to exactly one module.</p></div><button class="button button--ghost button--compact" data-modules-clear>${icon("fa-rotate-left")} Clear</button></div><div class="paper-card__body"><div class="registry-toolbar"><label class="search-field">${icon("fa-magnifying-glass")}<input data-modules-q type="search" value="${e(f.q??"")}" placeholder="Search pages, commerce, GTM, inventory..."></label><label><span class="sr-only">View</span><select data-modules-view><option value="modules" ${f.view!=="capabilities"?"selected":""}>Modules</option><option value="capabilities" ${f.view==="capabilities"?"selected":""}>Capabilities</option></select></label><label><span class="sr-only">Category</span><select data-modules-category><option value="">All categories</option>${cats}</select></label><label data-modules-dependency-wrap><span class="sr-only">Dependency kind</span><select data-modules-dependency><option value="">Any dependency</option>${deps}</select></label><label data-modules-mode-wrap><span class="sr-only">Capability mode</span><select data-modules-mode><option value="">Any capability mode</option>${modes}</select></label><div class="field-filter-summary"><span>Results</span><strong data-modules-count>${engine.search(f.q??"",f).length}</strong></div></div></div></section><section data-modules-results>${renderModuleResults(engine,f)}</section><section class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Dependency semantics</h2></div></div><div class="paper-card__body"><div class="field-rule-grid">${engine.data.dependencyKinds.map(x=>`<div><strong>${e(x.label)}</strong><p>${e(x.description)}</p></div>`).join("")}</div></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Reserved, not implemented</h2></div></div><div class="paper-card__body"><ul class="list-clean">${engine.reservedCapabilities.map(x=>`<li class="list-item"><span class="list-item__icon">${icon("fa-ban")}</span><div><code>${e(x.capabilityId)}</code><p>${e(x.reason)}</p></div></li>`).join("")}</ul></div></article></section></div>`;}
function chips(arr,cls=""){return arr?.length?`<div class="module-chip-list">${arr.map(x=>`<a class="tag ${cls}" href="${link(x)}"><code>${e(x)}</code></a>`).join("")}</div>`:`<p class="empty-note">None.</p>`;}
export function renderModuleDetail(engine,m){const caps=engine.capabilitiesFor(m.moduleId);const deps=m.dependencies.map(d=>`<div class="module-dependency"><span class="status-badge">${e(d.kind)}</span><a href="${link('modules.'+d.moduleId)}"><code>${e(d.moduleId)}</code></a><p>${e(d.reason)}</p></div>`).join("")||`<p class="empty-note">No module dependencies.</p>`;const nav=(title,arr)=>`<article class="paper-card"><div class="paper-card__header"><div><h2>${e(title)}</h2></div></div><div class="paper-card__body"><div class="module-nav-list">${arr.map(x=>`<div><span>${icon(x.icon)}</span><strong>${e(x.label)}</strong><code>${e(x.path)}</code><small>Order ${x.order}</small></div>`).join("")||'<p class="empty-note">No navigation entries.</p>'}</div></div></article>`;return `${h(m.name,m.description,`<div class="header-actions"><span class="status-badge status-badge--stable">${m.selection.mandatory?"Mandatory":"Selectable"}</span><button class="button button--secondary button--compact" data-copy-value="${e(m.moduleId)}" data-copy-label="Module ID copied">${icon("fa-copy")} Copy ID</button><a class="button button--secondary button--compact" href="./registry/modules/definitions/${e(m.moduleId)}.json">${icon("fa-file-code")} Source</a></div>`)}<div class="stack stack--lg"><section class="manifest-detail-grid"><div><span>Module ID</span><code>${e(m.moduleId)}</code></div><div><span>Category</span><strong>${e(m.category)}</strong></div><div><span>Capabilities</span><strong>${caps.length}</strong></div><div><span>Contracts</span><strong>${m.contractBindings.length}</strong></div><div><span>Permissions</span><strong>${m.permissionBindings.length}</strong></div><div><span>Events</span><strong>${m.eventBindings.length}</strong></div></section><article class="paper-card"><div class="paper-card__header"><div><h2>Dependencies</h2></div></div><div class="paper-card__body stack">${deps}</div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Capabilities</h2><p>Always-on and default-on capabilities form the module baseline; optional capabilities require explicit selection.</p></div></div><div class="paper-card__body">${caps.length?`<div class="module-chip-list">${caps.map(x=>`<a class="tag" href="${capabilityLink(x.$id)}"><code>${e(x.capabilityId)}</code></a>`).join("")}</div>`:`<p class="empty-note">None.</p>`}</div></article><div class="dashboard-grid dashboard-grid--2">${nav("Customer CMS navigation",m.cms.navigation)}${nav("NEXT F Admin navigation",m.admin.navigation)}</div><article class="paper-card"><div class="paper-card__header"><div><h2>Contract bindings</h2></div></div><div class="paper-card__body">${chips(m.contractBindings)}</div></article><div class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Permissions</h2></div></div><div class="paper-card__body">${chips(m.permissionBindings)}</div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Events</h2></div></div><div class="paper-card__body">${chips(m.eventBindings)}</div></article></div><article class="paper-card"><div class="paper-card__header"><div><h2>Authoritative module JSON</h2></div><button class="button button--ghost button--compact" data-copy-value="${e(JSON.stringify(m))}" data-copy-label="Module JSON copied">${icon("fa-copy")} Copy JSON</button></div><div class="paper-card__body"><pre class="code-block registry-raw"><code>${e(JSON.stringify(m,null,2))}</code></pre></div></article></div>`;}
export function renderCapabilityDetail(engine,c){const parent=engine.module(c.moduleId);return `${h(c.name,c.description,`<div class="header-actions"><span class="status-badge status-badge--stable">${e(c.mode)}</span><button class="button button--secondary button--compact" data-copy-value="${e(c.capabilityId)}" data-copy-label="Capability ID copied">${icon("fa-copy")} Copy ID</button><a class="button button--secondary button--compact" href="./registry/modules/capabilities/${e(c.moduleId)}/${e(c.capabilityId.split('.')[1])}.json">${icon("fa-file-code")} Source</a></div>`)}<div class="stack stack--lg"><section class="manifest-detail-grid"><div><span>Capability ID</span><code>${e(c.capabilityId)}</code></div><div><span>Parent module</span><a href="${link(parent.$id)}"><strong>${e(parent.name)}</strong></a></div><div><span>Mode</span><strong>${e(c.mode)}</strong></div><div><span>Selectable</span><strong>${c.selectable?"Yes":"No"}</strong></div><div><span>Default enabled</span><strong>${c.defaultEnabled?"Yes":"No"}</strong></div><div><span>Contracts</span><strong>${c.contractBindings.length}</strong></div></section><article class="paper-card"><div class="paper-card__header"><div><h2>Contract bindings</h2></div></div><div class="paper-card__body">${chips(c.contractBindings)}</div></article><div class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Permission bindings</h2></div></div><div class="paper-card__body">${chips(c.permissionBindings)}</div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Event bindings</h2></div></div><div class="paper-card__body">${chips(c.eventBindings)}</div></article></div><article class="paper-card"><div class="paper-card__header"><div><h2>Authoritative capability JSON</h2></div></div><div class="paper-card__body"><pre class="code-block registry-raw"><code>${e(JSON.stringify(c,null,2))}</code></pre></div></article></div>`;}
