import { icon, escapeHtml } from "./utils.js";
const e=escapeHtml;
const riskClass={standard:"stable",elevated:"draft",sensitive:"experimental",destructive:"danger",privileged:"danger"};
function header(title,desc,actions=""){return `<header class="page-header"><div class="page-header__copy"><p class="page-header__eyebrow">Platform</p><h1>${e(title)}</h1><p>${e(desc)}</p></div>${actions?`<div class="page-header__actions">${actions}</div>`:""}</header>`;}
function riskBadge(r){return `<span class="status-badge status-badge--${riskClass[r]||"draft"}">${e(r)}</span>`;}
function permissionCards(engine,filters){const rows=engine.search(filters.q??"",{...filters,view:"permissions"});if(!rows.length)return `<div class="registry-empty paper-card"><span class="registry-empty__icon">${icon("fa-key")}</span><h3>No permissions match</h3><p>Change the search or authorization filters.</p></div>`;return `<div class="permission-grid">${rows.map(p=>`<article class="paper-card permission-card"><div class="permission-card__top"><div><h3><a href="#/registry/item/${encodeURIComponent(p.permissionId)}">${e(p.name)}</a></h3><code>${e(p.permissionId)}</code></div>${riskBadge(p.riskLevel)}</div><p>${e(p.description)}</p><div class="permission-card__meta"><span class="tag">${e(p.scopeKind)}</span><span class="tag">${e(p.action)}</span>${p.customerEligible?`<span class="tag">Customer CMS</span>`:`<span class="tag">NEXT F only</span>`}${p.requiresRecentAuthentication?`<span class="tag">Recent auth</span>`:""}</div></article>`).join("")}</div>`;}
function roleCards(engine,filters){const rows=engine.search(filters.q??"",{...filters,view:"roles"});if(!rows.length)return `<div class="registry-empty paper-card"><span class="registry-empty__icon">${icon("fa-user-group")}</span><h3>No roles match</h3><p>Change the search.</p></div>`;return `<div class="permission-grid">${rows.map(r=>`<article class="paper-card permission-card"><div class="permission-card__top"><div><h3><a href="#/registry/item/${encodeURIComponent(r.$id)}">${e(r.name)}</a></h3><code>${e(r.roleKey)}</code></div><span class="status-badge status-badge--stable">${e(r.roleType)}</span></div><p>${e(r.description)}</p><div class="permission-card__meta"><span class="tag">${e(r.scopeKind)}</span><span class="tag">${r.permissionIds.length} explicit permissions</span><span class="tag">${r.assignableByCustomer?"Customer assignable":"Internal"}</span></div></article>`).join("")}</div>`;}
function schemaCards(engine,filters){const rows=engine.search(filters.q??"",{...filters,view:"schemas"});return `<div class="permission-grid">${rows.map(s=>`<article class="paper-card permission-card"><div class="permission-card__top"><div><h3><a href="#/registry/item/${encodeURIComponent(s.$id)}">${e(s.name)}</a></h3><code>${e(s.$id)}</code></div><span class="status-badge status-badge--stable">schema</span></div><p>${e(s.description)}</p><div class="permission-card__meta"><span class="tag">${s.fields.length} fields</span><span class="tag">${s.validationRules.length} rules</span></div></article>`).join("")}</div>`;}
export function renderPermissionsResults(engine,filters={}){if(filters.view==="roles")return roleCards(engine,filters);if(filters.view==="schemas")return schemaCards(engine,filters);return permissionCards(engine,filters);}
export function renderPermissionsIndex(engine,filters={}){const count=engine.search(filters.q??"",filters).length;const domains=engine.domains.map(x=>`<option value="${e(x)}" ${filters.domain===x?"selected":""}>${e(x)}</option>`).join("");const actions=engine.actions.map(x=>`<option value="${e(x.key)}" ${filters.action===x.key?"selected":""}>${e(x.label)}</option>`).join("");const scopes=engine.scopes.map(x=>`<option value="${e(x)}" ${filters.scope===x?"selected":""}>${e(x)}</option>`).join("");const risks=engine.risks.map(x=>`<option value="${e(x)}" ${filters.risk===x?"selected":""}>${e(x)}</option>`).join("");return `${header("Permissions","Canonical authorization IDs, reference roles, scope rules and deny-by-default evaluation contracts shared by NEXT F Admin, Customer CMS and APIs.",`<div class="header-actions"><span class="status-badge status-badge--stable">Phase 15</span><span class="tag">${engine.permissions.length} permissions</span><span class="tag">${engine.roles.length} roles</span></div>`)}<div class="stack stack--lg">
<div class="registry-source-bar" role="status"><span>${icon("fa-database")} Runtime source</span><strong>${engine.source==="authoritative-json"?"Authoritative Permission JSON":"Generated local fallback"}</strong><span class="tag">v${e(engine.data.registryVersion)}</span></div>
<section class="permission-hero"><article class="paper-card"><div class="paper-card__body"><h2>Exact permission. Exact scope. Deny by default.</h2><p>Authentication proves identity. The Permission Registry determines whether that principal may perform one protected action inside the requested Organization, Site or resource scope.</p><div class="permission-flow"><span>Principal</span><b>→</b><span>Role Assignment</span><b>→</b><span>Exact Permission</span><b>→</b><span>Scope Check</span><b>→</b><span>Allow / Deny</span></div></div></article><article class="paper-card"><div class="paper-card__body permission-summary-grid"><div><span>Permissions</span><strong>${engine.permissions.length}</strong></div><div><span>Reference roles</span><strong>${engine.roles.length}</strong></div><div><span>Support schemas</span><strong>${engine.schemas.length}</strong></div><div><span>Wildcard grants</span><strong>0</strong></div></div></article></section>
<div class="integration-boundary"><strong>UI visibility is never authorization.</strong><p>Customer CMS and NEXT F Admin may hide unavailable controls, but every protected server/API operation must independently evaluate the exact canonical permission and trusted scope.</p></div>
<section class="paper-card"><div class="paper-card__header"><div><h2>Find authorization definitions</h2><p>Search canonical permission IDs, roles and authorization support schemas.</p></div><button class="button button--ghost button--compact" type="button" data-permissions-clear>${icon("fa-rotate-left")} Clear</button></div><div class="paper-card__body"><div class="registry-toolbar"><label class="search-field">${icon("fa-magnifying-glass")}<input type="search" value="${e(filters.q??"")}" placeholder="Search content.pages.publish, orders, SEO..." data-permissions-q></label><label><span class="sr-only">View</span><select data-permissions-view><option value="permissions" ${!filters.view||filters.view==="permissions"?"selected":""}>Permissions</option><option value="roles" ${filters.view==="roles"?"selected":""}>Roles</option><option value="schemas" ${filters.view==="schemas"?"selected":""}>Support schemas</option></select></label><label data-permissions-domain-wrap><span class="sr-only">Domain</span><select data-permissions-domain><option value="">All domains</option>${domains}</select></label><label data-permissions-action-wrap><span class="sr-only">Action</span><select data-permissions-action><option value="">All actions</option>${actions}</select></label><label data-permissions-scope-wrap><span class="sr-only">Scope</span><select data-permissions-scope><option value="">All scopes</option>${scopes}</select></label><label data-permissions-risk-wrap><span class="sr-only">Risk</span><select data-permissions-risk><option value="">All risk levels</option>${risks}</select></label><div class="field-filter-summary"><span>Results</span><strong data-permissions-count>${count}</strong></div></div></div></section>
<section data-permissions-results>${renderPermissionsResults(engine,filters)}</section>
<section class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Customer role model</h2></div></div><div class="paper-card__body"><ul class="list-clean"><li class="list-item"><span class="list-item__icon">${icon("fa-user-shield")}</span><div><strong>Organization Owner</strong><p>Highest standard customer role, but never a NEXT F platform administrator.</p></div></li><li class="list-item"><span class="list-item__icon">${icon("fa-user-pen")}</span><div><strong>Narrow reference roles</strong><p>Website Admin, editors, SEO, commerce, marketing, lead management, support and viewer baselines.</p></div></li><li class="list-item"><span class="list-item__icon">${icon("fa-sliders")}</span><div><strong>Custom roles</strong><p>Customers may compose narrower roles only from eligible permissions within their own grant ceiling.</p></div></li></ul></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Authorization invariants</h2></div><a class="button button--secondary button--compact" href="./standards/23-permission-registry-standard.md">${icon("fa-file-lines")} Standard</a></div><div class="paper-card__body"><div class="field-rule-grid"><div><strong>No wildcards</strong><p>Future registry expansion cannot silently increase existing role power.</p></div><div><strong>No implied actions</strong><p><code>manage</code> does not automatically imply view, edit or delete.</p></div><div><strong>No direct grants</strong><p>Phase 15 uses Role Assignments rather than hidden per-user permission exceptions.</p></div><div><strong>Business rules still win</strong><p>Permission does not bypass publishing, commerce, retention, privacy or state-transition rules.</p></div></div></div></article></section></div>`;}

function permissionSource(p){return `./registry/permissions/permissions/${e(p.permissionId)}.json`;}
export function renderPermissionDetail(registry,item,p){const notes=(p.notes||[]).map(n=>`<li class="list-item"><span class="list-item__icon">${icon("fa-circle-info")}</span><div>${e(n)}</div></li>`).join("")||'<li class="list-item"><span class="list-item__icon">—</span><div>No additional notes.</div></li>';return `${header(p.name,p.description,`<div class="header-actions"><button class="button button--secondary button--compact" data-copy-value="${e(p.permissionId)}" data-copy-label="Permission ID copied">${icon("fa-copy")} Copy ID</button><a class="button button--secondary button--compact" href="${permissionSource(p)}">${icon("fa-file-code")} Source</a></div>`)}<div class="stack stack--lg"><section class="permission-detail-grid"><div class="permission-detail-cell"><span>Permission ID</span><code>${e(p.permissionId)}</code></div><div class="permission-detail-cell"><span>Domain</span><strong>${e(p.domain)}</strong></div><div class="permission-detail-cell"><span>Resource</span><strong>${e(p.resource)}</strong></div><div class="permission-detail-cell"><span>Action</span><strong>${e(p.action)}</strong></div><div class="permission-detail-cell"><span>Scope</span><strong>${e(p.scopeKind)}</strong></div><div class="permission-detail-cell"><span>Risk</span>${riskBadge(p.riskLevel)}</div></section><div class="dashboard-grid dashboard-grid--2"><article class="paper-card"><div class="paper-card__header"><div><h2>Eligibility</h2></div></div><div class="paper-card__body"><dl class="data-list"><div class="data-list__row"><dt>Customer CMS</dt><dd>${p.customerEligible?"Eligible":"Not eligible"}</dd></div><div class="data-list__row"><dt>NEXT F Admin</dt><dd>${p.adminEligible?"Eligible":"Not eligible"}</dd></div><div class="data-list__row"><dt>Customer custom-role grant</dt><dd>${p.grantable?"Allowed within grant ceiling":"Prohibited"}</dd></div><div class="data-list__row"><dt>Recent authentication</dt><dd>${p.requiresRecentAuthentication?"Required":"Not inherently required"}</dd></div><div class="data-list__row"><dt>Explicit confirmation</dt><dd>${p.requiresExplicitConfirmation?"Required for human action":"Not inherently required"}</dd></div></dl></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Surfaces</h2></div></div><div class="paper-card__body"><div class="permission-token-list">${p.surfaces.map(x=>`<span class="tag">${e(x)}</span>`).join("")}</div><h3>Authorization constraints</h3><div class="permission-token-list">${p.constraints.map(x=>`<code>${e(x)}</code>`).join("")}</div></div></article></div><article class="paper-card"><div class="paper-card__header"><div><h2>Implementation notes</h2></div></div><div class="paper-card__body"><ul class="list-clean">${notes}</ul></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Authoritative permission JSON</h2></div><button class="button button--ghost button--compact" data-copy-value="${e(JSON.stringify(p))}" data-copy-label="Permission JSON copied">${icon("fa-copy")} Copy JSON</button></div><div class="paper-card__body"><pre class="code-block registry-raw"><code>${e(JSON.stringify(p,null,2))}</code></pre></div></article></div>`;}
export function renderRoleDetail(engine,r){const perms=engine.permissionsForRole(r.$id);const grouped=new Map();for(const p of perms){if(!grouped.has(p.domain))grouped.set(p.domain,[]);grouped.get(p.domain).push(p);}return `${header(r.name,r.description,`<div class="header-actions"><button class="button button--secondary button--compact" data-copy-value="${e(r.roleKey)}" data-copy-label="Role key copied">${icon("fa-copy")} Copy key</button></div>`)}<div class="stack stack--lg"><section class="permission-detail-grid"><div class="permission-detail-cell"><span>Role key</span><code>${e(r.roleKey)}</code></div><div class="permission-detail-cell"><span>Type</span><strong>${e(r.roleType)}</strong></div><div class="permission-detail-cell"><span>Scope</span><strong>${e(r.scopeKind)}</strong></div><div class="permission-detail-cell"><span>Permissions</span><strong>${r.permissionIds.length}</strong></div><div class="permission-detail-cell"><span>Customer assignable</span><strong>${r.assignableByCustomer?"Yes":"No"}</strong></div><div class="permission-detail-cell"><span>Protected</span><strong>${r.systemProtected?"Yes":"No"}</strong></div></section>${[...grouped.entries()].map(([domain,list])=>`<article class="paper-card"><div class="paper-card__header"><div><h2>${e(domain)}</h2><p>${list.length} explicit permissions</p></div></div><div class="paper-card__body permission-list">${list.map(p=>`<a class="permission-list__item" href="#/registry/item/${encodeURIComponent(p.permissionId)}"><code>${e(p.permissionId)}</code><span>${e(p.name)}</span>${riskBadge(p.riskLevel)}</a>`).join("")}</div></article>`).join("")}<article class="paper-card"><div class="paper-card__header"><div><h2>Role JSON</h2></div><button class="button button--ghost button--compact" data-copy-value="${e(JSON.stringify(r))}" data-copy-label="Role JSON copied">${icon("fa-copy")} Copy JSON</button></div><div class="paper-card__body"><pre class="code-block registry-raw"><code>${e(JSON.stringify(r,null,2))}</code></pre></div></article></div>`;}
export function renderPermissionSchemaDetail(s){const rows=s.fields.map(f=>`<tr><td><code>${e(f.key)}</code></td><td>${f.primitive?`<a href="#/registry/item/${encodeURIComponent(f.primitive)}"><code>${e(f.primitive)}</code></a>`:f.schema?`<a href="#/registry/item/${encodeURIComponent(f.schema)}"><code>${e(f.schema)}</code></a>`:"—"}</td><td>${f.required?"Yes":"No"}</td><td>${e(f.description)}</td></tr>`).join("");return `${header(s.name,s.description)}<div class="stack stack--lg"><article class="paper-card"><div class="paper-card__header"><div><h2>Fields</h2></div></div><div class="paper-card__body table-wrap"><table class="data-table"><thead><tr><th>Field</th><th>Source</th><th>Required</th><th>Description</th></tr></thead><tbody>${rows}</tbody></table></div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Validation rules</h2></div></div><div class="paper-card__body stack">${s.validationRules.map(r=>`<div class="rule-row"><div class="rule-row__id">${e(r.id)}</div><div>${e(r.description)}</div></div>`).join("")}</div></article><article class="paper-card"><div class="paper-card__header"><div><h2>Authoritative JSON</h2></div></div><div class="paper-card__body"><pre class="code-block registry-raw"><code>${e(JSON.stringify(s,null,2))}</code></pre></div></article></div>`;}
