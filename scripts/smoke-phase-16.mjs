import fs from "node:fs";import path from "node:path";
const root=process.cwd(),fail=[],pass=[];const c=(n,x)=>x?pass.push(n):fail.push(n);
const html=fs.readFileSync(path.join(root,"index.html"),"utf8"),router=fs.readFileSync(path.join(root,"js/router.js"),"utf8"),app=fs.readFileSync(path.join(root,"js/app.js"),"utf8"),routes=fs.readFileSync(path.join(root,"js/routes.js"),"utf8"),page=fs.readFileSync(path.join(root,"js/site-manifest-pages.js"),"utf8");
c("site manifest css",html.includes("./css/site-manifest.css"));c("version chip",html.includes("v0.17.0"));c("site manifest route available",routes.includes('path: "/development/site-manifest"')&&routes.includes('phase: 16, status: "available"'));c("app loads manifests",app.includes("loadSiteManifests"));c("router page",router.includes("renderSiteManifestIndex"));c("manifest schema detail",router.includes("renderSiteManifestSchemaDetail"));c("manifest example UI",page.includes("data-manifest-example"));c("manifest JSON schema link",page.includes("nextf-site-manifest.schema.json"));c("no webhook recursion",!router.includes("function bindWebhookCopy(engine){\n  bindWebhookCopy(engine);"));
const ids=[...html.matchAll(/\sid="([^"]+)"/g)].map(x=>x[1]);c("no duplicate html IDs",new Set(ids).size===ids.length);
for(const m of html.matchAll(/(?:href|src)="\.\/([^"#?]+)"/g)){const p=m[1];if(/^https?:/.test(p))continue;c(`asset ${p}`,fs.existsSync(path.join(root,p)));}
console.log(`Phase 16 smoke passes: ${pass.length}`);console.log(`Phase 16 smoke failures: ${fail.length}`);if(fail.length){fail.forEach(x=>console.error(`- ${x}`));process.exit(1)}console.log("PASS - Phase 16 portal smoke test passed.");
