import fs from "node:fs";import path from "node:path";import crypto from "node:crypto";
const root=process.cwd();const version=fs.readFileSync(path.join(root,"VERSION"),"utf8").trim();const indexPath=path.join(root,"registry/modules/index.json");const index=JSON.parse(fs.readFileSync(indexPath,"utf8"));if(index.registryVersion!==version)throw new Error("Module registry version does not match VERSION");const raw=fs.readFileSync(indexPath);const digest=crypto.createHash("sha256").update(raw).digest("hex");fs.writeFileSync(path.join(root,"js/generated-modules.js"),`// Generated from registry/modules/index.json\n// SHA-256: ${digest}\nexport const GENERATED_MODULES_SOURCE_SHA256 = ${JSON.stringify(digest)};\nexport const GENERATED_MODULES = ${JSON.stringify(index)};\n`);const rp=path.join(root,"registry/registry.json");const registry=JSON.parse(fs.readFileSync(rp,"utf8"));const rr=fs.readFileSync(rp);const rd=crypto.createHash("sha256").update(rr).digest("hex");fs.writeFileSync(path.join(root,"js/generated-registry.js"),`// Generated from registry/registry.json\n// SHA-256: ${rd}\nexport const GENERATED_REGISTRY_SOURCE_SHA256 = ${JSON.stringify(rd)};\nexport const GENERATED_REGISTRY = ${JSON.stringify(registry)};\n`);console.log(`Module Registry synchronized: ${index.modules.length} modules, ${index.capabilities.length} capabilities.`);
