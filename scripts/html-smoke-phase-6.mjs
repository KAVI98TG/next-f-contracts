import fs from "node:fs";const html=fs.readFileSync("index.html","utf8");const ids=[...html.matchAll(/\sid="([^"]+)"/g)].map(m=>m[1]);const dup=[...new Set(ids.filter((id,i)=>ids.indexOf(id)!==i))];const refs=[...html.matchAll(/(?:href|src)="\.\/([^"#?]+)"/g)].map(m=>m[1]).filter(x=>!/^https?:/.test(x));const missing=refs.filter(x=>!fs.existsSync(x));console.log(`Duplicate HTML IDs: ${dup.length}`);console.log(`Missing local assets: ${missing.length}`);if(dup.length||missing.length){if(dup.length)console.error(dup);if(missing.length)console.error(missing);process.exit(1)}console.log("PASS - HTML structure smoke test.");
