import fs from "node:fs";import path from "node:path";const root=process.cwd(),fail=[],pass=[];const c=(n,x)=>x?pass.push(n):fail.push(n);const html=fs.readFileSync(path.join(root,"index.html"),"utf8"),router=fs.readFileSync(path.join(root,"js/router.js"),"utf8"),app=fs.readFileSync(path.join(root,"js/app.js"),"utf8"),routes=fs.readFileSync(path.join(root,"js/routes.js"),"utf8");c("permissions css",html.includes("./css/permissions.css"));c("version chip",html.includes("v0.16.0"));c("permission route available",routes.includes('path: "/platform/permissions"')&&routes.includes('phase: 15, status: "available"'));c("app loads permissions",app.includes("loadPermissions"));c("router page",router.includes("renderPermissionsIndex"));c("permission detail",router.includes("renderPermissionDetail"));c("role detail",router.includes("renderRoleDetail"));c("no webhook recursion",!router.includes("function bindWebhookCopy(engine){\n  bindWebhookCopy(engine);"));const ids=[...html.matchAll(/\sid="([^"]+)"/g)].map(x=>x[1]);c("no duplicate html IDs",new Set(ids).size===ids.length);for(const m of html.matchAll(/(?:href|src)="\.\/([^"#?]+)"/g)){const p=m[1];if(/^https?:/.test(p))continue;c(`asset ${p}`,fs.existsSync(path.join(root,p)));}console.log(`Phase 15 smoke passes: ${pass.length}`);console.log(`Phase 15 smoke failures: ${fail.length}`);if(fail.length){fail.forEach(x=>console.error(`- ${x}`));process.exit(1)}console.log("PASS - Phase 15 portal smoke test passed.");
