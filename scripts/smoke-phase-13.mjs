import fs from 'node:fs';import path from 'node:path';import { spawnSync } from 'node:child_process';
const root=path.resolve(process.cwd());const files=['index.html','404.html','js/app.js','js/router.js','js/routes.js','js/events-registry-engine.js','js/events-pages.js','js/generated-events.js','css/events.css','registry/events/index.json','registry/events/producers.json','registry/events/consumers.json','standards/21-event-registry-standard.md'];const pass=[],fail=[];const check=(n,v,d='')=>v?pass.push(n):fail.push(`${n}${d?`: ${d}`:''}`);for(const f of files)check(`Exists ${f}`,fs.existsSync(path.join(root,f)));
for(const rel of ['js/app.js','js/router.js','js/routes.js','js/events-registry-engine.js','js/events-pages.js','js/generated-events.js']){const r=spawnSync(process.execPath,['--check',path.join(root,rel)],{encoding:'utf8'});check(`JS syntax ${rel}`,r.status===0,(r.stderr||'').trim());}
const html=fs.readFileSync(path.join(root,'index.html'),'utf8');const ids=[...html.matchAll(/\sid="([^"]+)"/g)].map(x=>x[1]);check('No duplicate HTML IDs',ids.length===new Set(ids).size);const local=[...html.matchAll(/(?:href|src)="(\.\/[^"#?]+)"/g)].map(x=>x[1]);for(const rel of local)check(`Local asset ${rel}`,fs.existsSync(path.resolve(root,rel)),rel);check('Event route sidebar',html.includes('#/events/registry'));check('Light-only token',fs.readFileSync(path.join(root,'css/tokens.css'),'utf8').includes('color-scheme: light'));
console.log('\nNEXT F Phase 13 repository smoke test');console.log(`Passes: ${pass.length}`);console.log(`Failures: ${fail.length}`);if(fail.length){console.error(fail.map(x=>`- ${x}`).join('\n'));process.exit(1)}console.log('\nPASS - repository and Event Registry portal wiring smoke test.');
