import fs from "node:fs";import path from "node:path";import {execFileSync} from "node:child_process";const root=process.cwd(),fail=[],pass=[];const ok=(n,c,d="")=>c?pass.push(n):fail.push(`${n}${d?`: ${d}`:""}`);
for(const f of fs.readdirSync(path.join(root,"js")).filter(x=>x.endsWith(".js"))){try{execFileSync("node",["--check",path.join(root,"js",f)],{stdio:"pipe"});pass.push(`JS ${f}`)}catch{fail.push(`JS syntax ${f}`)}}const html=fs.readFileSync(path.join(root,"index.html"),"utf8");ok("Examples nav",html.includes("#/development/examples"));ok("Examples CSS",html.includes("css/examples.css"));ok("Current version",html.includes("v0.23.0"));ok("Phase 22 sidebar",html.includes("Phase 22"));ok("Light theme",html.includes('<meta name="color-scheme" content="light">'));const css=fs.readFileSync(path.join(root,"css/examples.css"),"utf8");ok("No backdrop blur",!css.includes("backdrop-filter"));ok("No dark theme",!css.includes("prefers-color-scheme: dark"));const ids=[...html.matchAll(/\sid="([^"]+)"/g)].map(x=>x[1]);ok("No duplicate HTML IDs",new Set(ids).size===ids.length);const app=fs.readFileSync(path.join(root,"js/app.js"),"utf8");ok("App loads Examples",app.includes("loadExamplesRegistry"));const router=fs.readFileSync(path.join(root,"js/router.js"),"utf8");ok("Examples runtime route",router.includes('route.path === "/development/examples"'));ok("Example detail renderer",router.includes("renderExampleDetail"));console.log(`NEXT F Contracts Phase 22 smoke\nPasses: ${pass.length}\nFailures: ${fail.length}`);if(fail.length){console.error(fail.map(x=>`- ${x}`).join("\n"));process.exit(1)}console.log("PASS - Examples portal/runtime smoke test complete.");
