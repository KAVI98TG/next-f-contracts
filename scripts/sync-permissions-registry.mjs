import fs from "node:fs";import path from "node:path";import crypto from "node:crypto";
const root=process.cwd();const src=path.join(root,"registry/permissions/index.json");const out=path.join(root,"js/generated-permissions.js");const raw=fs.readFileSync(src);const data=JSON.parse(raw.toString("utf8"));const digest=crypto.createHash("sha256").update(raw).digest("hex");fs.writeFileSync(out,`// Generated from registry/permissions/index.json\n// SHA-256: ${digest}\nexport const GENERATED_PERMISSIONS_SOURCE_SHA256 = ${JSON.stringify(digest)};\nexport const GENERATED_PERMISSIONS = ${JSON.stringify(data)};\n`);console.log(`Permissions fallback synchronized: ${data.permissions.length} permissions, ${data.roles.length} roles, ${data.schemas.length} schemas.`);
