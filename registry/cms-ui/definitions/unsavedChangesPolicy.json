{
  "$id": "cmsUi.unsavedChangesPolicy",
  "name": "Unsaved Changes Policy",
  "version": "0.20.0",
  "status": "stable",
  "domain": "cms-ui",
  "category": "workflow",
  "description": "Defines dirty-state, autosave and navigation warning behavior.",
  "fields": [
    {
      "key": "id",
      "required": true,
      "nullable": false,
      "description": "Stable UI metadata identifier.",
      "primitive": "fields.text"
    },
    {
      "key": "metadata",
      "required": true,
      "nullable": false,
      "description": "Structured metadata payload governed by the Customer CMS UI Metadata Standard.",
      "primitive": "fields.json"
    }
  ],
  "relationships": [
    {
      "type": "implements",
      "target": "cms-ui.customerCmsUiMetadataStandard",
      "description": "Governed by the Customer CMS UI Metadata Standard."
    }
  ],
  "validationRules": [
    {
      "id": "cmsUi.unsavedChangesPolicy.stableId",
      "description": "Machine identifiers remain stable within a major contract version."
    }
  ],
  "cms": {
    "customerVisible": false,
    "adminVisible": true
  },
  "delivery": {
    "publicAllowed": true,
    "notes": "Developer metadata; contains no customer secrets."
  }
}
