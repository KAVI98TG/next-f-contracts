{
  "$id": "api.public-interaction",
  "apiId": "api.public-interaction",
  "name": "Public Interaction API",
  "version": "1.0.0",
  "status": "stable",
  "basePath": "/api/v1/public/sites/{siteId}/interactions",
  "description": "Strict public write/read interactions such as form submission and public site search. Kept separate from Public Content so published delivery stays read-only.",
  "defaultAuthMode": "anonymous",
  "defaultRateLimitClass": "public-write",
  "defaultCachePolicy": "no-store",
  "defaultCorsPolicy": "site-origin",
  "moduleBindings": [
    "core",
    "forms"
  ],
  "operationIds": [
    "api.public-interaction.submit-form",
    "api.public-interaction.search",
    "api.public-interaction.begin-upload",
    "api.public-interaction.complete-upload"
  ],
  "publicDocumentation": true,
  "order": 20,
  "compatibility": {
    "contractVersionRange": ">=0.19.0 <1.0.0",
    "manifestBindingRequired": true
  },
  "boundaries": [
    "Uses canonical NEXT F Contract Registry schemas rather than redefining domain entities.",
    "Secrets and privileged credentials are never returned through generic API responses.",
    "API Version and NEXT F Contract Version are independently explicit."
  ]
}
